# CLAUDE.md — 유쌤에듀 HR 프로그램 개발 가이드

Next.js 14 (App Router) + TypeScript + Prisma(PostgreSQL/Supabase) HR 관리 웹앱.
배포: Vercel + Supabase (서버리스). 로컬/Codespaces는 로컬 Postgres. 자세히는 docs/DEPLOY.md.

## 명령어
- `npm run dev` — 개발 서버 (http://localhost:3000)
- `npm run build && npm start` — 프로덕션
- `npm test` — 급여/연차 엔진 단위 테스트 (Vitest)
- `npm run seed` / `npm run db:reset` — 시딩 / DB 초기화+재시딩
- `npm run db:clear-employees` — 직원 데이터 전체 삭제 미리보기 (실제 삭제는 `CONFIRM=DELETE` 필요).
  설정(회사·요율·세액표·공휴일)은 남긴다.
- `npm run db:fix-contracts` — 계약 이력 앞뒤 맞추기 미리보기 (실제 반영은 `APPLY=1`). 멱등.
- `npm run db:holidays-sync` — 공휴일 표 채우기 (올해·내년. `YEARS=2026,2027`). `HOLIDAY_API_KEY` 필요. 멱등.
- `node scripts/db-deploy.mjs` — 배포용 스키마 반영 (Vercel 빌드가 자동 실행. 안전장치 포함)
- `npx tsc --noEmit` — 타입 체크
- 로그인 비밀번호: `.env` 의 `ADMIN_PASSWORD` (기본 `yoossam2025`)

## 아키텍처
- **엔진(순수 함수, DB 무관, 테스트 있음)**: `lib/payroll.ts`(급여), `lib/leave.ts`(연차, 근로기준법 §60),
  `lib/overtime.ts`(보강 오버타임, 근로기준법 §56).
  UI/API 는 이 엔진을 호출만 한다. 계산 로직 변경은 여기서.
- **앱 아이콘**: `app/icon.svg`(파비콘) 가 원본이고, `app/apple-icon.png`·`public/icon-*.png` 는
  거기서 뽑은 래스터다 — 모양을 바꾸면 SVG 를 고친 뒤 PNG 를 다시 뽑아야 한다.
  `app/manifest.ts` 로 홈 화면 추가(PWA)를 지원한다. 학원 로고(`Company.logo`)와는 별개다 —
  로고는 가로로 길어 16px 파비콘으로는 읽히지 않는다.
- **학원 로고**: `Company.logo` 에 **data URI 로 담는다**(파일 아님) — 서버리스는 쓸 수 있는 파일 경로가
  없고 재배포 없이 화면에서 바꿔야 하기 때문. 설정 화면(`components/LogoUpload.tsx`)이 브라우저에서
  긴 변 320px 로 줄여 올리므로 원본 png 를 그대로 골라도 된다. 문서 머리글은 `logoImg(company)`,
  화면은 사이드바·로그인에서 쓴다. 없으면 회사명만 나온다.
- **법인 인감(직인)**: `Company.stamp` — 로고와 같은 data URI 방식(`components/StampUpload.tsx`,
  `POST/DELETE /api/settings/stamp`, 검증은 `lib/company-image.ts` 를 로고와 공유).
  찍히는 곳은 **회사가 날인하는 자리뿐**이다: 계약서 "갑" 의 대표자(`signDuo`)와 재직·경력증명서의
  대표이사. 서약서·동의서는 직원이 회사에 내는 문서라 회사 도장을 찍지 않고, 계약서 "을" 성명 옆도
  본인이 직접 날인할 자리라 `(인)` 글자로 남긴다(`companySeal()` / `stampImg()`, 테스트 있음).
  인감이 없으면 예전처럼 `(인)`·`(직인)` 글자만 나온다.
  **자리를 차지하지 않게 절대배치**한다(`.stamp-anchor` 가 0×0, lib/pdf.ts) — 실물 도장처럼 이름 위에
  겹쳐 찍히면서 줄 높이를 밀지 않는다. 자리를 차지하면 2페이지로 맞춰 둔 계약서가 3페이지로 늘어난다.
  크기는 `STAMP_SIZE_MM`(lib/pdf-seam.ts) **하나가 정한다** — 본문 날인·간인·`.compact` 축소본이
  모두 여기서 파생되므로 **CSS 에 숫자를 다시 적지 않는다**. 지금은 **28mm**(compact 24mm).
  - ⚠ **이 값은 이미지를 담는 상자 크기이지 찍히는 붉은 원의 지름이 아니다.** 올린 PNG 에는
    도장 둘레에 투명 여백이 있고 `object-fit:contain` 이 여백까지 상자에 맞추므로, 실제 잉크는
    그보다 작게 나온다. 지금 인감은 잉크가 상자의 **6할 남짓**이다.
  - 맞춘 근거(2026-08): 실물 법인인감을 자로 재니 **지름 18mm**. 서명된 계약서에 실물과 시스템
    인감이 나란히 찍힌 것을 대조하니 실물이 시스템(상자 20mm)의 약 **1.4배**였다 —
    그때 찍힌 잉크가 13mm 남짓이었다는 뜻이라, 18mm 를 내려면 상자가 **28mm** 여야 한다.
  - **여백 없이 다시 자른 인감으로 갈아 끼우면 이 값도 함께 내려야 한다**(상자 = 잉크가 되므로
    18~20mm). 바꾼 뒤에는 한 장 뽑아 **자로 재 볼 것**.
  - 크기를 바꿔도 **찍히는 자리(중심)는 그대로**다 — CSS 가 모서리가 아니라 중심 좌표로 잡는
    이유다. 모서리 기준이면 지름을 키울 때마다 도장이 오른쪽·아래로 흘러 이름과 주소 잔글씨를 덮는다.
  증명서는 `-78%` 로 더 올려 상호·대표이사 사이에 걸치게 한다(기본값이면 밑의 주소 잔글씨를 덮는다).
  업로드는 **반드시 PNG 로 내보낸다** — JPG 면 투명 배경이 검게 채워져 이름을 가린다.
  **이미 발급된 PDF 는 바뀌지 않는다**(발급 시점에 찍히므로 다시 발급해야 한다).
- **간인(間印)**: 계약서가 2장을 넘으면 장 경계마다 인감을 반씩 걸쳐 찍는다
  (`lib/pdf-seam.ts`, 테스트 있음. `Company.stampSeam` 로 끄고 켠다 — 설정 화면).
  앞장 오른쪽 끝에 왼쪽 자락, 뒷장 왼쪽 끝에 오른쪽 자락 — 종이를 접어 찍으면 그 자리에서 맞물린다.
  보이는 폭은 절반(10mm)이 아니라 **8mm** 다. 접힌 자리가 도장 가운데를 먹으므로 실제로도 자락만
  남고, 절반을 그대로 얹으면 본문을 그만큼 더 덮는다.
  **몇 장인지 렌더 전에는 알 수 없어 두 번 뽑는다** — 먼저 한 번 뽑아 `countPdfPages()` 로 세고,
  2장 이상일 때만 간인·쪽번호를 얹어 다시 뽑는다(1장이면 첫 결과를 그대로 쓴다).
  쪽번호(`1 / 2`)는 puppeteer `footerTemplate` 로 아래 여백에 찍는다 — 본문에 넣으면 마지막 줄과
  겹치고, 머리글/바닥글 틀은 본문과 **다른 문서**라 한글 폰트가 없어 숫자와 `/` 만 쓴다.
  **계약서 단독 발급(`genContract`)에만 붙인다** — 신규입사 패키지는 서로 다른 문서 4종의 묶음이라
  문서 경계를 넘는 간인은 뜻이 달라진다.
  간인은 **법적 의무가 아니다**(근로기준법 §17 은 서면 명시·교부만 요구). 인쇄된 간인은 매 장에
  같은 그림이 찍히므로 증거력도 없다 — 관행상의 모양을 옮긴 것이고, 실제 증거력은 출력 후 사람이
  직접 찍는 도장에서 나온다.
- **각 장 이니셜란**: 2장 이상인 계약서의 아래 여백에 `갑 (서명) ____  을 (서명) ____` 줄을 넣는다
  (`footerTemplate()` in `lib/pdf-seam.ts`, `Company.pageInitials` 로 끄고 켠다).
  **간인보다 이쪽이 곧바르다** — 민사소송법 §358 은 「사문서는 본인 또는 대리인의 서명이나 날인 또는
  무인이 있는 때에는 진정한 것으로 추정한다」고 정한다. 추정이 붙는 건 **그 장에 있는 서명**이고,
  간인은 장들이 이어졌다는 정황만 만든다.
  **본문이 아니라 아래 여백(puppeteer `footerTemplate`)에 그린다** — 본문에 넣으면 마지막 줄과 겹치고,
  자리를 비우려 여백을 넓히면 쪽 나눔이 달라져 2장짜리가 3장이 된다. 여백에 그리면 쪽 나눔이 그대로다.
  **바닥글 틀에는 폰트를 따로 심어야 한다**(`footerFontCss()`) — 틀은 본문과 다른 문서라 본문에 심은
  폰트가 닿지 않고, 서버리스 Chromium 에는 시스템 한글 폰트가 없어 한글이 빈칸으로 나간다.
  로컬에는 우연히 있어(WenQuanYiZenHei) 안 심어도 되는 것처럼 보이니 주의.
- **문서→PDF**: `lib/documents.ts`(계약서/서약서/동의서), `lib/documents-pay.ts`(명세서/증명서) 가
  HTML 을 만들고 `lib/pdf.ts` 가 puppeteer-core 로 PDF 렌더(Vercel=@sparticuz/chromium, 로컬=설치된 Chrome/Edge).
  한글폰트는 `assets/fonts/` 를 base64 임베드(`lib/fonts.ts`).
- **DB 어댑터**: `lib/repo.ts` 가 Prisma 레코드 ↔ 엔진 입력/문서 입력 변환. 회사정보·요율·세액표 로딩.
- **계약 = 보수조건의 단일 진실**: `lib/contracts.ts`. 급여 산정·계약서 발급·화면 표시 모두
  `governingContract(contracts, asOf)` 로 그 시점 계약을 찾아 쓴다.
- **작업 이력**: `lib/activity.ts` 의 `logActivity()` → `AuditLog`. 급여·명세서·계약·직원·문서처럼
  되돌리기 어려운 작업은 여기에 한 줄 남긴다(화면: `/activity`). 기록 실패가 본 작업을 막지 않는다.
- **서비스**: `lib/payroll-service.ts`(월 급여 upsert), `lib/leave-service.ts`(승인/조정),
  `lib/makeup-service.ts`(보강 신청·확인·오버타임 산정), `lib/doc-service.ts`(PDF 생성+저장+기록),
  `lib/email.ts`, `lib/scheduler.ts`, `lib/slack.ts`.
- **API**: `app/api/**` — 모두 `isAuthed()` 가드(슬랙/크론 제외, 자체 서명검증).
- **화면**: `app/(app)/**` — 서버컴포넌트가 데이터 로드, `components/*Client.tsx` 가 상호작용.
  **긴 명단 화면(`/payroll`·`/employees`·`/leave`)은 화면 높이에 맞춰 두 층으로 나눈다** —
  연·월 선택·검색·필터·합계·표 머리글은 고정이고 **행만 표 안에서 스크롤**한다.
  수십 명이 넘어가면 아래로 내려갈수록 어느 열·입력칸인지 분간이 안 돼 잘못 적기 쉬웠다.
  페이지 뿌리를 `flex flex-col h-[calc(100dvh-…)] min-h-[28rem]` 로 잡고(창이 짧으면 예전처럼
  페이지째 스크롤된다) 표 카드가 `flex-1 min-h-0 overflow-auto` 로 남은 높이를 먹는다.
  머리글은 `.sticky-head`(globals.css) — **`thead` 와 `th` 에 둘 다** 건다. thead 째로 붙여야
  연차 표처럼 **두 줄짜리(rowSpan) 머리글**이 겹치지 않고, thead sticky 를 모르는 옛 브라우저에서는
  th 쪽이 첫 줄만이라도 붙여 준다. 머리글 아래 경계선은 border 가 아니라 inset shadow 로 그린다 —
  sticky 셀의 border 는 스크롤할 때 같이 밀려 사라진다.
  긴 설명 문단(급여 계산 규칙·연차 규칙)은 `<details>` 로 접어 둔다 — 400px 짜리가 늘 펼쳐져
  있으면 정작 볼 행이 두어 줄만 남는다. 연차의 *승인 대기* 칸도 `max-h` 로 묶어 표를 밀어내지 않게 한다.
  보강 화면(`/makeup`)만 표가 아니라 **월 달력**(`components/MakeupCalendar.tsx`) — 누가 언제 보강하는지가
  날짜 축으로 보여야 하고, 구글 보강캘린더와 같은 그림을 보게 하기 위함.
- **달력의 '오늘' 칸**은 연차·보강이 **같은 모양**을 쓴다(`TODAY_CELL`/`TODAY_NUM`, components/ui.tsx).
  칸에 옅은 배경 + **안쪽** 테두리(`ring-inset` — 바깥 ring 은 격자를 흔든다), 날짜 숫자에 채운 동그라미.
  - **오늘 날짜는 서버에서 구해 넘긴다**(`kstTodayYmd`, lib/format.ts). 클라이언트에서 `Date.now()` 로
    구하면 서버가 그린 HTML 과 달라져 하이드레이션이 어긋난다(자정 언저리에 실제로 갈린다).
  - ⚠ **Tailwind 클래스 문자열을 `lib/` 에 두지 않는다.** `content` 가 `app/**`·`components/**` 만
    훑어서(tailwind.config.ts) `lib/` 에 적으면 클래스가 아예 생성되지 않고 **조용히 아무 효과도
    나지 않는다**(겪었다 — 칸 배경·테두리가 통째로 빠졌다).
- **연차 달력**(`lib/leave-calendar.ts` 순수 함수·테스트 있음 + `components/LeaveCalendar.tsx`).
  연차 화면은 **표/달력 탭**(`components/LeaveViews.tsx`)으로 가른다 — 표는 '누가 얼마나 남았나',
  달력은 '그날 누가 자리에 없나' 다. 방학·연휴처럼 여러 사람이 겹치는 날은 표로는 안 보인다.
  **아래로 이어 붙이지 않고 탭으로 가른 이유**: 이 화면은 창 높이에 맞춘 두 층 구조라 표 밑에
  달력을 또 두면 둘 다 반쯤만 보인다. 고른 탭은 브라우저에 기억한다(`yoossam.table.leave.view`).
  - **원장만으로는 못 그린다.** 슬랙 신청 승인은 `approveLeaveRequest()` 가 **시작일 하나에
    총 일수**로 한 줄만 남기고(lib/leave-service.ts), 관리자 직접 반영(`/api/leave/adjust`)은
    `businessDaysFrom()` 로 **하루씩 쪼개** 남긴다. 원장만 그리면 3일 휴가가 첫날 한 칸으로 뭉친다.
    그래서 **신청서(기간)를 날짜별로 펼치고**, 원장은 **신청서에 매이지 않은 줄만**
    (`requestId == null`) 쓴다 — 매인 줄은 신청서의 그림자라 함께 그리면 같은 날이 두 번 나온다.
  - 펼칠 때 **주말·공휴일을 건너뛴다**(반영 경로와 같은 규칙). 반차는 펼치지 않는다.
    반려·취소분과 **부여(+)는 빼고**(그날 쉰 것이 아니다), 승인 대기·취소 요청은 낸다.
  - **평일 휴무는 연차가 아니다**(`lib/dayoff.ts` 순수 함수·테스트 있음 + `lib/dayoff-service.ts`,
    `DayOff` 모델). 운영팀이 그 주 토요일 당번 근무 대신 평일 하루를 쉬는 것이라 근로시간을
    옮긴 것뿐이다. 그래서 **`LeaveTransaction`(연차 원장)에 넣지 않고 따로 둔다** — 원장에 넣으면
    `summarizeLeave` 가 훑어 잔여가 조용히 줄어든다. 달력에는 `pool: "DAYOFF"`(주황)로 나오되
    **'이 달 연차 사용' 합계에서 빠진다**(`deductsFromBalance` 하나로 판정).
    - 원본은 **구글 연차 캘린더의 `(휴무)김수민` 일정**이고 사람이 손으로 넣는다. 앱은 **읽기만** 한다.
      `휴무` 가 없는 제목은 건드리지 않는다 — 같은 캘린더에 앱이 올린 연차 일정(`김서준 연차`)이
      함께 있어서, 아무 제목이나 집으면 연차를 휴무로 두 번 세게 된다.
    - **구글 종일 일정의 `end.date` 는 배타적**이다(8/12 하루 = start 8/12, end 8/13).
      그대로 훑으면 하루씩 더 쉬는 것으로 잡힌다.
    - **캘린더가 진실이라 사라진 줄은 지운다**(공휴일 표와 반대). 단 **가져온 창 안에서,
      캘린더에서 온 줄만** — 관리자가 직접 넣은 줄(`source="MANUAL"`)과 창 밖 기록은 남는다.
    - **못 읽으면 아무것도 하지 않는다** — `listEvents` 는 실패할 때 `null` 을 준다(빈 배열이 아니다).
      빈 배열로 받으면 '휴무가 하나도 없다' 로 읽고 표를 통째로 지운다.
    - 이름을 못 찾은 일정(퇴사자·오타)은 **버리지 않고 화면에 띄운다**(`dayOffWarning`) —
      조용히 빠지면 그 사람 휴무가 달력에서 사라진 것을 아무도 모른다.
  - **대시보드의 '1주일 내 휴가·휴무'** 도 같은 함수를 쓴다(`upcomingLeave`). 연달아 붙은 날은
    `groupConsecutive` 로 한 줄로 묶고 **이어짐은 근무일 기준**이라 주말을 낀 휴가가 두 토막으로
    갈라지지 않는다. **승인 대기도 함께** 낸다 — 모레 시작인데 결재가 안 된 건이 가장 급한데
    생성일 순인 승인 대기 목록에서는 눈에 안 띈다.
- **대시보드 월별 급여 추이**: 집계는 `lib/payroll-trend.ts`(순수 함수, 테스트 있음),
  그림은 `components/PayrollTrendChart.tsx`. **차트 라이브러리를 쓰지 않고 인라인 SVG** 로
  그린다 — 점이 많아야 수십 개(월 단위)라 의존성을 들일 만큼 복잡하지 않다.
  - **급여 레코드가 있는 달만 낸다.** 없는 달을 0으로 채우면 '아직 산정하지 않은 달' 이
    '급여가 0원이었던 달' 로 읽힌다. 그래서 중간에 빈 달이 있으면 축에서 그냥 빠진다.
  - 갈래는 넷(총계·지급항목별·급여형태별·부서별)이고 **어느 갈래로 보든 월 합계가 같다**.
    항목 분해가 총계와 어긋나면 그래프를 믿을 수 없게 되므로 테스트로 못박아 둔다.
  - **'계약 기본급' 에는 포괄임금 약정 시간외·야간이 들어간다**(`contractBaseOf`) —
    계약서에 이미 있는 월 급여의 일부다. 그 달 새로 생긴 오버타임만 '오버타임수당' 으로 뺀다.
    판정은 세무 시트·퇴직급여와 **같은 함수**(`fixedOvertimeOf`/`variableOvertimeOf`)를 쓴다.
  - 부서는 급여 레코드에 없어 **직원 카드에서 가져온다** — 지금 부서 기준이라 과거에 부서를
    옮긴 사람은 옛 달도 지금 부서로 잡힌다.
  - 계열을 좁혀도 **월 합계는 그 달 전체**를 그대로 둔다(급여 화면 합계 카드와 같은 원칙).
  - 세로 눈금의 위끝은 `niceMax()` 로 **최댓값보다 조금 위의 깔끔한 수**로 올린다 —
    최댓값을 그대로 쓰면 선이 천장에 붙고 눈금이 `1,734만 / 867만` 처럼 읽히지 않는다.
    바닥은 **언제나 0** 이다(최솟값에 맞춰 자르면 5% 차이가 절벽처럼 보인다).
- **명단 화면의 필터·정렬**: 계산부는 `lib/table-sort.ts`·`lib/table-filter.ts`(순수 함수, 테스트 있음),
  화면 껍데기는 `components/TableTools.tsx`(`useTableSort` / `SortTh` / `FilterSelect` / `FilterBar`).
  수십 건 규모라 서버를 다시 부르지 않고 브라우저에서 거른다 — 서버컴포넌트가 행을 평평한
  값으로 직렬화해 내려주고(`EmployeeRow` / `LeaveRow`) 표 컴포넌트가 걸러 정렬한다.
  날짜는 `YYYY-MM-DD` 문자열로 넘겨 사전순 = 시간순이 되게 한다.
  라벨로 보여주는 값(근속 "1년 6개월")은 정렬용 원시값을 따로 싣는다.
  - **정렬 조건을 여러 개 쌓는다**(`SortKeys` = `SortState[]`, 앞이 1순위).
    그냥 클릭은 **그 열 하나로** 정렬하고(오름 → 내림 → 원래 순서),
    **Shift(⌘/Ctrl)+클릭**은 조건을 **뒤에 덧붙인다**(오름 → 내림 → 그 조건만 빼기).
    조건이 여럿일 때 그냥 클릭하면 **바로 끄지 않고 그 열 하나로 좁힌다** — 그래야 쌓인 데서
    빠져나올 길이 있다. 둘 이상이면 머리글에 **순번 배지**가 붙고 필터 줄에 `정렬 N단계` 가 뜬다
    (툴팁이 순서를 풀어 준다. 열 이름은 각 표의 `SORT_LABELS`).
  - **빈 값은 오름·내림과 무관하게 언제나 뒤로** 간다 — `sortRows` 가 방향을 곱하기 **전에**
    가려낸다. `compare()` 결과에 −1 을 곱하면 빈칸이 맨 위로 올라와 볼 것 없는 행이 첫 줄을
    차지한다(옛 단일 정렬이 실제로 그랬다). 둘 다 비면 그 조건은 '같다' 로 보고 다음 조건으로 넘어간다.
  - **`!!sort` 로 '정렬 중인가' 를 판정하지 않는다** — 빈 배열도 참이다. 훅이 돌려주는
    `hasSort` 를 쓴다(`dirty` 계산). 필터도 같은 이유로 `anyFilterActive(f)` 를 쓴다.
  - **필터도 여러 개를 함께 고른다**(`FilterValues` = `string[]`. 부서 = 교수부 + 조교팀).
    **빈 배열이 '전체'** 다 — '아무것도 안 보임' 으로 읽으면 화면이 통째로 빈다.
    판정은 `matchesFilter(고른값, 행의값)` 하나로 하고, 값이 아니라 '비어 있음' 을 고르는
    `__none`(부서 미지정)만 호출부가 따로 본다.
    네이티브 `<select multiple>` 을 쓰지 않는다 — 목록이 펼쳐진 채 자리를 차지하고,
    여러 개를 고르려면 Ctrl/⌘ 를 눌러야 한다는 걸 아무도 모른다. **체크박스 목록**을 띄우고
    맨 위 *전체* 는 고르는 값이 아니라 **모두 지우기**다. 버튼에는 `교수부 외 2` 로 줄여 적는다
    (다 늘어놓으면 필터 줄이 두 층이 된다).
  - 저장 형식이 배열로 바뀌었으므로 **옛 저장값을 받아 준다** — 정렬은 단일 `{key,dir}`,
    필터는 단일 문자열(`""` = 전체)이었다. `normalizeSort` / `normalizeFilter`(묶음은
    `normalizeFilterSet`)를 `useStoredState` 의 `normalize` 인자로 넘긴다.
    안 그러면 브라우저에 남은 옛 값으로 화면이 깨진다.
  **고른 필터와 정렬은 브라우저에 기억한다**(`useStoredState`, localStorage `yoossam.table.*`).
  서버가 아니라 브라우저에 두는 이유: 관리자 로그인이 **비밀번호 공유** 방식이라 서버에 저장하면
  모두가 같은 값을 쓰게 된다 — 계정 모델이 생기면 그때 서버로 옮긴다.
  - **첫 그림은 반드시 기본값으로 그린다**(저장값은 `useEffect` 로 얹는다) — 서버가 그린 HTML 과
    달라지면 하이드레이션이 어긋난다. 잠깐 기본 상태가 보였다 바뀌는 건 그 대가다.
  - 저장값은 `{v: …}` 로 감싼다 — 정렬은 `null`(원래 순서)도 저장할 값이라 '저장한 적 없음' 과
    구분해야 한다. 없는 항목은 기본값으로 메워(`merged`) 나중에 필터가 늘어도 옛 저장값이 안 깨진다.
  - **기본값으로 돌아오면 키를 지운다** — 안 그러면 *초기화* 직후 저장 이펙트가 기본값을 도로
    써 넣어 기억이 영영 안 없어진다.
  - **이름 검색칸은 기억하지 않는다** — 그때그때 한 사람 찾는 동작이지 기본값이 아니다.
    걸러진 화면을 전체로 오해하지 않게 뭔가 걸려 있으면 `기억됨` 배지와 *초기화* 를 함께 띄운다.
  - 급여 화면은 **보던 연·월(`payroll.period`)도 기억**한다 — 한 달치 작업을 며칠에 걸쳐
    여러 번 드나들며 하기 때문. *초기화* 는 필터·정렬만 되돌리고 연·월은 건드리지 않는다
    (연·월은 필터가 아니라 따로 고르는 값이다).
  - 급여 표의 **합계 카드는 거른 것과 무관하게 그 달 전체**를 보여 준다 — 은행·세무로 나가는
    실제 총액이라 화면을 걸렀다고 줄어들면 안 된다. 대신 필터가 걸려 있으면 `· 전체 기준` 을 붙인다.
    **거른 묶음의 소계는 표 맨 아래 고정 줄(`tfoot.sticky-foot`)** 에 따로 보여 준다 —
    금액이 지급액·공제액·실수령 열 밑에 그대로 정렬돼야 읽히므로 카드가 아니라 표 안에 둔다.
    카드(전체)와 소계(거른 묶음)를 한 화면에서 나란히 보게 하는 것이 목적이다.

## 규칙/주의
- **보수조건은 계약(Contract)만 고친다.** 직원 카드(Employee)의 기본급·수당·위탁비율·인센티브·
  급여형태·세무구분은 '오늘 시점 지배 계약' 을 비추는 거울일 뿐이라 직접 수정하지 않는다
  (`PATCH /api/employees/[id]` 는 인적사항만 받고 보수 필드는 무시).
  변경 경로는 둘: 신규 계약 작성(`POST /api/contracts`) 또는 기존 계약 수정(`PATCH /api/contracts/[id]`).
  두 경로 모두 끝에 `refreshEmployeeCard()` 로 카드를 다시 맞춘다.
- **계약 기간에 빈틈을 만들지 않는다.** 계약을 만들거나 고치거나 지운 뒤에는 반드시
  `normalizeContractTimeline(employeeId)` 로 그 직원의 이력 전체를 다시 맞춘다 —
  뒤 계약이 시작하면 앞 계약을 '그 전날' 로 닫고, 종료일이 지난 계약은 EXPIRED 로 내린다
  (`planContractTimeline()` 이 순수 함수, 테스트 있음). 신규 계약 하나만 보고 직전 것을 닫는
  방식으로는 이미 끝난 계약의 상태나 과거 날짜로 끼워 넣은 계약이 정리되지 않았다.
  **종료일을 늘리지는 않는다** — 덮이지 않은 빈 기간은 `contractIssues()` 가 화면에 경고로 띄우고
  사람이 판단한다. 화면 표시는 저장된 status 가 아니라 `effectiveContractStatus(c, now)` 를 쓴다
  (시간이 지나면 저장값이 뒤처지므로). 기존 데이터는 `npm run db:fix-contracts` 로 한 번 훑는다.
- 미래 시작 계약을 만들어도 발효일 전까지는 카드·급여에 반영되지 않는다(지배 계약이 아직 이전 계약).
- 이식성을 위해 Prisma **enum 대신 문자열** + `lib/constants.ts` 의 상수/라벨 사용.
- DB는 Postgres. 스키마 변경 시 `npx prisma db push`(DIRECT_URL 사용). 서버리스 런타임은 pgbouncer(DATABASE_URL).
  **Vercel 배포는 빌드가 알아서 맞춘다** — `vercel-build` 가 `scripts/db-deploy.mjs` 를 먼저 돌린다
  (Vercel 은 `build` 보다 `vercel-build` 를 우선한다. 로컬 `npm run build` 는 그대로 DB 를 건드리지 않는다).
  DB 주소가 없거나 preview/development 배포거나 `SKIP_DB_PUSH=1` 이면 조용히 건너뛴다.
  **`--accept-data-loss` 는 일부러 넣지 않았다** — 데이터가 있는 열을 지우는 변경이면 빌드가
  멈추고 배포가 나가지 않는다(조용히 지우느니 멈추는 게 낫다). 그럴 땐 로컬에서 확인 후 직접 반영한다.
- 서버리스(Vercel)에서는 파일 저장 대신 PDF를 버퍼로 스트리밍/첨부. 예약발송은 Vercel Cron → `/api/cron`.
- **급여 시트에 누가 오르는지는 '그 달 재직 기간' 하나로 정한다**(`lib/payroll-roster.ts` 순수 함수·테스트 있음).
  `employedInMonth()` 는 **`prorationRatioFor(...) > 0` 과 같은 판정**이어야 한다(테스트로 못박아 둔다) —
  어긋나면 시트에는 올라오는데 금액이 0 이거나 그 반대가 된다.
  - **전월 자 퇴직자는 익월 시트에서 자동으로 빠진다.** 산정이 건너뛰는 것만으로는 부족했다 —
    월초에 급여를 한 번 돌린 뒤 퇴직일을 입력하면 **이미 만들어진 기록이 그대로 남아**
    세무 제출자료·은행 이체 파일까지 흘러갔다. 그래서 **전체 산정 때마다 남은 기록을 훑어 내린다**
    (`pruneResignedFromSheet()`). 한 사람만 지목해 저장할 때는 훑지 않는다 — 남의 행이 사라지면
    무슨 일이 일어난 건지 알 수 없다.
  - **발송(SENT)된 기록은 내리지 않고 알린다** — 명세서가 이미 직원에게 갔으므로 사유를 남기는
    '발송 잠금 해제' 를 거쳐야 한다. **직원 카드를 못 찾은 기록도 건드리지 않는다**(근거 없이 지우지 않는다).
  - **지운 금액을 작업 이력에 남긴다**(`PAYROLL_PRUNE`) — 퇴직일 오타 하나로 공제 입력값까지
    사라지므로 무엇이 얼마짜리였는지 되짚을 수 있어야 한다. 화면도 제외된 사람을 그때 알린다.
  - **퇴직자를 일부러 올릴 수도 있다**(`POST /api/payroll/add`, 화면의 *＋ 직원 추가*) —
    마지막 급여 뒤에 확정된 보강 수당, 미사용 연차수당, 인센티브 정산처럼 남은 지급분이 있다.
    올린 기록에는 `PayrollRecord.manualAdd` 가 새겨져 **배치가 다시 내리지 않는다**.
    일할계산은 0% 라 **기본급·수당은 0원**이고 직접 넣은 항목만 지급된다 — 창이 그 사실을 먼저 적는다.
    되돌리는 길(`DELETE /api/payroll/[id]`, 행의 *내리기 ✕*)은 **직접 추가한 행에만** 연다.
    재직자 행까지 지울 수 있으면 다음 산정에서 도로 생겨 지운 셈도 안 되면서 그 사이 세무·이체 파일만 어긋난다.
  - **상여·미사용연차는 배치 재산정에서 보존한다**(인센티브·매출과 같은 이유). 수동 추가한 퇴직자 행은
    기본급이 0 원이라 **이 두 칸이 지급액의 전부**다 — 화면을 거치지 않는 재산정에서 지워지면 정산분이 통째로 사라진다.
  - **화면은 '그 달' 이 아니라 '오늘' 기준으로 퇴직자를 표시한다**(`resignStatusOf`). 8월 시트에
    8/15 퇴직자가 있는 건 맞는 일이지만(마지막 급여) 9월에 그 화면을 열면 이미 나간 사람이고,
    이름만 봐서는 재직자와 구분이 안 돼 **엉뚱한 사람에게 급여가 나갈 수 있다**. 그래서 행을
    붉게 칠하고 이름 옆에 `2026-08-15 퇴직` 을 적는다. 아직 오지 않은 퇴사일은 `퇴사 예정`(노랑)으로
    갈라 적는다 — 같은 말로 적으면 이미 나간 줄 안다.
    표 위에 **몇 명이 얼마짜리인지**를 한 줄로 띄우고(행마다 붙은 표시는 스크롤해야 보인다),
    **돈이 실제로 나가는 은행 이체 파일**을 받을 때도 같은 경고를 낸다. 둘 다 **막지는 않는다** —
    마지막 급여와 퇴직 정산은 정상적으로 실려야 한다.
    합계 카드와 같은 원칙으로 **거른 것과 무관하게 그 달 전체**를 센다(걸렀다고 사라지면 경고가 아니다).
    오늘 날짜는 **서버에서 구해 넘긴다**(`kstTodayYmd` → `PayrollClient` 의 `today` prop).
- **명세서를 발송하면 그 달 기록이 잠긴다(SENT)** — 재산정·공제 수정·재발송에서 모두 빠진다.
  **잠금은 풀 수 있다**(`unlockPayroll()`, `POST /api/payroll/[id]/unlock`) — 실무에서 정정은 생기고,
  못 풀게 두면 DB 를 직접 만지게 되어 오히려 기록이 안 남는다. 대신 **사유를 반드시 받고**
  `AuditLog`(`PAYROLL_UNLOCK`)에 누가·언제·왜·풀 때의 금액까지 남긴다.
  **일반 상태 변경(`PATCH /api/payroll/[id]` 의 `status`)으로는 SENT 를 풀 수 없다** — 거기서
  조용히 DRAFT 로 되돌릴 수 있으면 정정 이력이 남지 않아 잠금이 있으나 마나가 된다.
  화면은 **상태 필터를 '발송완료' 로 좁혔을 때만** 해제 버튼을 보여준다(평소엔 눌릴 일이 없다).
  **되돌려도 직원 메일함의 옛 명세서는 사라지지 않는다.** 그래서 `reissueCount` 를 올리고
  `firstSentAt`(최초 발송 시각. `emailedAt` 은 보낼 때마다 덮인다)을 새겨,
  다시 보낸 메일 제목에 `[정정]`, 명세서에 **정정 발급 (제n차)** 과 최초 발급일을 찍는다 —
  같은 달 명세서가 두 통 남으므로 어느 것이 최종본인지 **문서만 보고** 가려낼 수 있어야 한다
  (근로기준법 §48 교부). 테스트 있음(`lib/documents-reissue.test.ts`).
  해제 사유는 **내부 감사용이라 명세서·메일에 싣지 않는다**(관리자가 적는 메모라 직원에게 나갈 글이 아니다).
  세무사무소 제출자료·은행 이체 파일을 이미 넘긴 뒤면 금액이 바뀌므로 다시 받아야 한다 — 해제 창이 안내한다.
- 금액은 정수(원). 공제는 10원 절사(`floor10`). 통상시급은 `(주소정+주휴)*4.345` 환산시간 기준.
- **월급제·인센티브의 `baseWage` 는 '월 지급 총액'이고 식대·차량유지비(비과세)가 그 안에 포함**된다.
  엔진은 `baseP = baseWage − 식대 − 차량유지비` 로 과세분만 기본급에 싣고 비과세분을 따로 표시하므로
  합계는 baseWage 그대로다(400만 입력 + 식대 20만 → 지급계 400만, 과세 380만).
  직책수당은 총액에 **가산**되는 별도 항목. 시급제·비율제는 기본급 개념이 달라 식대를 별도 가산으로 둔다.
- **포괄임금(고정OT)은 금액이 아니라 '시간'을 계약에 저장**한다 — `fixedBaseHours`(기본급 산정시간, 예 209),
  `fixedOtHours`(약정 시간외), `fixedNightHours`(약정 야간). 금액은 `inclusiveWageBreakdown()`(lib/payroll.ts)이
  기준급여에서 역산한다: 통상시급 = **기준급여 ÷ (기본급시간 + 1.5×시간외 + 0.5×야간)**,
  시간외 = 통상시급×1.5×시간외시간, 야간 = 통상시급×0.5×야간시간, 기본급 = 기준급여 − 비과세 − 시간외 − 야간(잔액).
  기본급을 잔액으로 두므로 **항목 합계는 항상 기준급여와 일치**한다.
  **잔액은 반드시 '반올림한' 항목들을 빼서 구한다**(`target − 비과세 − 시간외 − 야간`, 테스트 있음) —
  반올림 전 값으로 빼면 각 항목이 흘린 소수점 잔차가 기본급에 남지 않고 지급계로 새어
  계약 총액과 1원씩 어긋난다(340만 계약 → 3,399,999원). 일할계산(`prorate`)이 걸리면
  식대만 있어도 같은 현상이 나므로 `target = round0(기준급여 × prorate)` 를 먼저 잡고 거기서 뺀다. 실제 서명된 계약서(하수정·최은희·김지연)를
  원 단위까지 재현하며, 계약서 제4조와 급여명세서가 같은 함수를 쓴다. 월급제·인센티브에만 적용(시급제·비율제는 무시).
  약정시간을 넘긴 실근로만 그 위에 추가 가산된다.
- 공제 기본모드: 4대보험 직원 **MANUAL**(세무사 지정값 직접입력), 사업소득 프리랜서 **AUTO**(3.3%).
  배치 재실행 시 수동값·실비·기타공제 항목(otherItems) 보존. 일할계산은 모드 무관 자동.
- **직원 정보에 넣어 두면 매달 자동으로 잡히는 공제가 둘 있다.**
  - **월 정기주차 공제** — `Employee.parkingFee`(월 정기주차 비용)의 **50%**(`parkingDeductionOf()`, 10원 절사).
    나머지 절반은 회사 부담. 보수조건이 아니라 복리후생이라 계약이 아닌 **직원 정보**에 둔다
    (`PATCH /api/employees/[id]` 가 받는다). 배치는 **저장된 값이 0일 때만** 기본값을 채운다 —
    그래야 주차비를 나중에 넣어도 다음 산정에서 반영되고, 그 달만 고친 값은 재산정해도 살아남는다.
    **음수(−) 를 넣을 수 있다** — 앞달 과다공제분 환급처럼 되돌릴 때 쓴다(공제계가 줄어 실수령이 늘어난다).
    **직원 정보의 `parkingFee` 자체도 음수를 받는다** — 매달 돌려줄 몫이 있으면 (−)로 넣어 두면
    공제가 매달 (−)로 잡힌다. 절사는 부호가 아니라 **크기** 기준이라(`-55,555 → -27,770`)
    환급액이 절사 때문에 되레 커지지 않는다. 부양가족수·기타비과세는 그대로 음수를 막는다.
    명세서 문구도 양수면 '50% 본인 부담분', 음수면 '환급' 으로 갈린다.
    0 은 '아직 안 넣음' 으로 보므로 재산정 때 기본값이 다시 채워진다 — 그 달만 빼려면 (−)로 상계한다.
  - **퇴직유보금** — 인센티브 계약자에게 **인센티브 원천액**의 1/12(8.3%)를 항상 자동. 확인서 제6조.
    **상여금(`bonusP`)은 대상이 아니다** — 그 칸은 비정기 특별상여 자리라 평균임금에 안 들어가는
    경우가 많다. 인센티브는 반드시 `인센티브` 칸(학생수 자동산정 + 직접 입력)에 넣는다.
    인센티브 칸이 없던 시절 상여 칸에 넣던 흔적이 있어, **인센티브 계약자인데 상여만 있고
    인센티브가 0인 행**은 급여 화면이 경고를 띄운다(그대로 두면 유보금이 안 잡힌다).
    **여기만 10원 절사를 하지 않고 원 단위로 반올림**한다 — 세무사무소에 넘기던 시트가 그렇게
    적립해 왔다(200,000 → 16,667원. 절사하면 16,660원이라 시트와 어긋난다).
    유보금은 법정공제가 아니라 직원 몫으로 떼어 두는 돈이라 깎지 않는 쪽이 맞기도 하다.
    **위탁계약(프리랜서)은 퇴직급여 대상이 아니라 제외**된다(`isContractorContract`).
    세무구분이 사업소득이어도 위탁 체크가 없으면 유보 대상이다 — 둘은 별개다.
  둘 다 명세서에 근거를 적는다(주차비: 월 정기주차 × 50%, 유보금: 별도 통장 송금).
- **퇴직급여는 별도 화면(`/severance`)에서 산정한다** — `lib/severance.ts`(순수 함수, 테스트 있음)
  + `lib/severance-service.ts`(DB 어댑터) + `SeverancePolicy`(단일 행 설정).
  유쌤에듀는 **근속 1년이 지나면 DC형 퇴직연금에 가입**한다.
  - **1년 전은 충당금, 1년 후는 부담금**(`severanceVerdict`). 용어가 다른 이유가 있다 —
    충당금은 아직 나가지 않는 회계상 적립이고, **부담금**은 근로자퇴직급여보장법 §20① 이 쓰는
    말로 실제로 근로자 계정에 납입하는 돈이다.
  - **충당금은 없어지는 돈이 아니다.** 계속근로가 1년을 넘기면 퇴직급여는 **입사일부터 전체
    기간**에 대해 지급 의무가 생기므로(§8①), 충당금 누계가 곧 **DC 가입 시 소급 납입할 몫**이다.
    그래서 화면이 그 달 금액만이 아니라 **누계를 함께** 보여주고, DC 로 넘어간 사람 옆에
    `소급 …원` 을 붙인다.
  - **대상 제외 둘**: 위탁계약(`isContractorContract`)과 **주 소정근로 15시간 미만**
    (§4① 단서 초단시간근로자). 후자는 급여형태와 무관하게 소정근로시간으로만 본다.
  - **근로시간표가 비면 '제외' 가 아니라 '판정 보류'(UNKNOWN)** 다 — 0시간을 초단시간으로 읽어
    조용히 빼면 퇴직급여가 통째로 안 쌓인다. 화면이 인원수와 함께 경고한다.
  - **산정기준 임금 = 계약서에 합의된 월 급여총액**이다. 상여·인센티브·**그 달 발생한**
    오버타임은 빼고, **포괄임금 약정 시간외·야간은 넣는다**(모두 설정으로 켤 수 있다).
    **인센티브를 빼는 건 누락이 아니다** — 인센티브분은 이미 `퇴직유보금`(원천액의 1/12)으로
    별도 통장에 적립하고 있어 여기서 또 넣으면 이중 적립이다. 그래서 표에 유보금 열을 나란히 둔다.
  - **오버타임은 두 갈래이고 급여 레코드에서는 한 칸에 섞여 있다.** `overtimeP`·`nightP` 에는
    계약서 제4조의 **약정분**(매달 같은 금액)과 그 달 새로 생긴 **변동분**이 함께 들어 있어
    금액만으로는 가릴 수 없다. 그 달 입력·확정된 **시간에서 변동분을 다시 세우고**
    (`variableOvertimeOf`) 나머지를 약정분으로 본다(`fixedOvertimeOf`, 둘 다 lib/payroll.ts).
    **세무사무소 제출자료의 '오버타임수당' 열과 같은 함수를 쓴다** — 판정을 둘로 두면 두 문서가
    어긋난다. 그래서 판정을 엔진(payroll.ts)에 두고 payroll-export 는 재수출만 한다.
    약정분을 빼면 산정기준이 계약 월 급여보다 적어지므로 **그쪽을 더 무겁게 경고**한다
    (매달 일어나는 어긋남이다).
    ⚠ **변동분을 빼는 것도 소지는 있다** — §20① 의 하한은 '연간 **임금총액**의 1/12' 이고
    연장·야간·휴일수당도 근로의 대가인 임금이다. 그 수당이 **실제로 발생한 달에만** 경고를
    띄운다(`underMinimumWarning`) — 발생하지도 않은 항목으로 매달 경고하면 경고가 무뎌진다.
    판단은 노무 자문의 몫이라 **막지는 않는다**.
  - 월 적립액 = 산정기준 임금 ÷ 12, **원 단위 반올림**(10원 절사하지 않는다 — 퇴직유보금과 같은
    이유로 근로자 몫이라 깎지 않는다).
  - **저장하지 않고 그때그때 산정한다** — 그 달 급여에서 파생되는 값이라 급여를 정정하면
    (잠금 해제 → 재산정) 따라 바뀌어야 한다. 저장해 두면 정정한 달이 옛 금액으로 남아 누계가
    조용히 어긋난다. **예외가 둘**이고 그건 `SeveranceMonthlyBase` 에 담는다(아래).
  - **월별 산정기준 임금의 출처는 셋이고 우선순위가 있다** — `MANUAL`(관리자 지정) >
    **그 달 급여 레코드** > `ESTIMATED`(계약 추산) > 없음.
    사람이 고친 값이 가장 세고, 실제 급여가 있으면 추산보다 그쪽이 맞다 —
    추산은 '급여가 없을 때 메우는 값' 이라 실제 급여를 이기면 안 된다.
    그래서 **나중에 그 달 급여가 생기면 추산값은 저절로 밀려난다**(지울 필요가 없다).
  - **누계는 급여 레코드가 아니라 '달' 을 돈다** — 입사한 달부터 대상 달까지 한 달씩 훑으며
    출처를 가린다. 급여 레코드만 훑던 방식은 **도입 이전(추산으로 메운) 달을 통째로 빠뜨렸다**.
  - **도입 이전 기간은 계약에서 추산해 채운다** — `npm run db:severance-migrate`
    (미리보기 기본, 실제 반영은 `APPLY=1`, `THROUGH=2026-06` 로 종료월 지정).
    `estimateContractBase()` 가 **그 달을 지배한 계약**(`governingContract`)의 보수조건으로
    월 급여총액을 만든다 — 월급제·인센티브는 `baseWage + positionAllow`(식대·차량·약정OT 가
    baseWage 안에 있으므로 **또 더하지 않는다**), 시급제는 `시급 × (주소정+주휴) × 4.345`
    에 식대·차량을 **별도 가산**한다(기본급 개념이 다르다). 테스트가 이 값이
    실제 급여에서 뽑은 `severanceBase()` 와 **같은 금액**임을 못박아 둔다 — 두 방식이 섞여도
    누계가 매끄러워야 하기 때문이다.
    **멱등**하다: 이미 값이 있는 달은 건너뛰고 `MANUAL` 은 `REDO=1` 로도 덮지 않는다.
    입·퇴사월만 일할계산하고 나머지는 **만근 가정**이라 결근·그 달만의 조정은 안 잡힌다.
  - **관리자가 월별 기준급여를 직접 고칠 수 있다**(`PUT/DELETE /api/severance/base`,
    화면에서 행 클릭 → *기준급여 수정*). 지정하면 그 달 급여보다 우선하므로 **되돌리는 길**을
    함께 둔다(*지정 해제*) — 지정해 두고 잊으면 나중에 급여를 정정해도 옛 값에 묶인다.
    지정·추산값에는 항목별 내역이 없어 **법정 하한 경고를 띄우지 않는다**(근거가 없다).
  - **산입 범위를 상수가 아니라 설정으로 둔 이유**: 무엇을 임금총액에 넣을지는 노무 자문으로
    바뀔 수 있는 판단이다. 상수면 바꿀 때마다 배포해야 하고, 무엇을 뺐는지가 화면에 남지 않는다.
    `severanceBase()` 가 산입·제외 항목을 **금액과 사유까지** 돌려주므로 상세 패널이 그대로 편다.
- 완전비율제(RATIO)에 **최저보장(ratioMinGuarantee)** 이 있으면 `max(매출×비율, 보장액)` 으로 지급한다
  (계약서 제5조 — 만근 조건 미충족 시에는 관리자가 조정).
- **위탁계약(프리랜서)은 근로기준법 항목을 통째로 뺀다** — `Contract.isContractor`(체크칸)가 진실이고
  `Employee.isContractor` 는 거울이다. 판정은 **반드시 `isContractorContract(e)`**(lib/constants.ts)로 한다:
  `isContractor === true || payScheme === "RATIO"` — 완전비율제는 위탁계약서라 성질상 언제나 포함된다.
  **새 제외 지점에 `payScheme === "RATIO"` 를 직접 쓰지 않는다**(시급제 위탁이 빠진다).
  빠지는 것: **주휴수당(§55)·연차(§60)·퇴직유보금·4대보험·연장/야간/휴일 가산(§56)·연차미사용수당·포괄임금 분해**.
  남는 것은 계약서의 **시급 또는 비율**뿐이다. 주 15시간을 넘겨도 주휴가 붙지 않는다 —
  15시간은 '근로자' 일 때 따지는 기준이라, 시급제 프리랜서에게 주휴가 붙던 것이 이 플래그가 없어 생긴 문제였다.
  세무구분(`incomeType`)과 **별개**다 — 사업소득으로 신고해도 실질이 근로자면 근로기준법이 적용되므로
  세금 처리 방식만 보고 자동 판단하지 않는다. 다만 위탁인데 세무구분이 EMPLOYEE 로 남아 있으면
  **보험료를 떼지 않고 3.3% 로 처리한 뒤 안내를 남긴다**(조용히 4대보험을 떼는 쪽이 더 위험하다).
- **연차는 1주 소정근로시간 15시간 이상일 때만 발생**한다(근로기준법 §18③ 초단시간근로자 제외).
  판정은 `isLeaveEligible(주소정시간, Employee.leaveEligible)` — 카드의 `leaveEligible` 이
  null 이면 근로시간으로 자동, true/false 면 계약대로 강제. `summarizeLeave(..., {eligible})` 에 넘긴다.
  미적용이어도 관리자가 직접 부여한 분(ADJUST +)은 살아 있다.
  **슬랙도 같은 판정을 쓴다** — `leaveBalanceOf()` 가 스케줄에서 판정해 `eligible` 을 넘기고
  `eligibility`(사유·주 소정시간)를 함께 돌려준다. 여기서 빠뜨리면 화면은 0인데 슬랙만 15일로 보인다.
  미적용 직원이 신청을 시도하면 `leaveBlockNotice()` 가 사유와 함께 막는다 —
  다만 관리자 부여분·대휴가 남아 있으면 그 범위 안에서는 쓸 수 있으므로 막지 않는다.
- **연차 신청은 슬랙으로만 들어온다.** 관리자는 신청서를 만들지 않고 `/api/leave/adjust` 로 바로 반영한다
  (연차 화면의 *+ 연차 반영*). 반영 시 당사자에게 슬랙 DM 을 보낼지 선택할 수 있고, 알림 실패가 반영을 되돌리지 않는다.
  **직원은 여러 명 고른다**(`employeeIds[]`) — 방학처럼 한꺼번에 넣는 일이 잦다. 완전비율제는 자동 제외.
  **사용분을 2일 이상 넣으면 시작일부터 평일 기준으로 하루씩 나눠 기록**한다
  (`businessDaysFrom()`, 주말·공휴일 건너뜀. 부여(+)는 하루에 몰아 넣는다).
  어느 날짜에 들어갈지는 공휴일 표를 봐야 알 수 있어 폼이 `preview:true` 로 같은 API 를 불러 제출 전에 보여준다.
  **구글 캘린더에는 올리지 않는다** — 방학은 전 직원이 같은 날 쉬어 캘린더가 도배된다(슬랙 승인 경로만 올린다).
- **슬랙 잔여 확인에는 사용 내역이 함께 나간다.** `leaveBalanceOf()` 가 트랜잭션을 그대로 돌려주고
  `leaveBalanceText(..., txns)` 가 `leaveUseLines()` 로 이번 연차기간 사용분을 한 줄씩 붙인다.
  최대 12줄이고 넘치면 **오래된 쪽을 접는다**(궁금한 건 방금 쓴 연차). 연차기간이 해를 넘기므로 날짜에 연도를 쓴다.
- **보강·주말근무 사전신청 = 오버타임 수당의 원장**(`MakeupSession`). 직원이 슬랙으로 등록하고
  (승인 절차 없음) **실근무 시각이 확정(CONFIRMED)** 돼야 비로소 수당이 생긴다. PLANNED 는 0원.
  - **주말근무는 별도 컬럼이 아니라 카테고리(`WEEKEND`)** 다 — 한 건은 어차피 오버타임 원장의
    한 줄이고, 카테고리가 이미 ① 수당 반영 기본값 ② 내신 상한 적용 여부를 가르는 자리다.
    컬럼을 따로 두면 둘이 어긋난다. 판정은 `isWeekendWork(category)` 하나로 한다.
    교수부가 아닌 직원의 주말 근무 신청이라 대상반·수강인원을 묻지 않고(슬랙 모달이 갈린다)
    **보강캘린더에도 올리지 않는다** — 보강을 보려고 보는 달력에 섞이면 보강이 안 읽힌다.
  - **슬랙 입구를 둘로 나눈 이유**: 슬랙 모달은 선택값에 따라 스스로 다시 그려지지 않는다
    (`views.update` 로 서버가 다시 밀어야 한다). 한 모달에서 고르게 하면 보강에만 있는 칸이
    주말근무 신청자에게도 보인다. 어느 입구였는지는 제출값이 아니라 `private_metadata` 로 나른다.
  - **실근무 확정은 관리자가 아니라 신청자가 한다**(`confirmMakeupActuals`, `by:"EMPLOYEE"`).
    관리자가 몰아서 처리하면 정작 몇 시에 끝났는지 아는 사람이 입력하지 않는다.
    **근무가 끝난 날의 다음날 00:00 부터** 열리고(`confirmOpensAt`. 근무 중에 미리 확정하면
    '예정' 을 그대로 베끼게 된다), **그 달의 다음 달 1일까지** 닫힌다(`confirmClosesAt`) —
    급여 산정이 월초에 시작되므로 그 전에 확정분이 다 들어와 있어야 한다. 늦게 받아 봐야
    이미 발송(SENT)된 달은 재산정하지 않아 수당에 닿지 않고, 조용히 받아 두면
    '확정했는데 왜 수당이 없냐' 가 된다.
    ⚠ **월말 근무는 창이 하루뿐이다**(8/31 근무 → 9/1 하루). 그래서 확정을 재촉하는 자리마다
    `confirmDeadlineLabel()` 로 **날짜를 박아 준다**(요청 DM·확정 모달·목록) — '언젠가 하면
    되겠지' 로 읽히면 그대로 놓친다. 놓친 건은 관리자가 `/makeup` 에서 대신 확정한다.
    확정 화면에는 **예정이 아니라 실제 시간을 적으라는 안내**(`honestyNotice`)를 입력칸보다 앞에 둔다.
    테스트 있음(`lib/makeup-confirm.test.ts`).
  - **근무를 하지 않았으면 신청자가 직접 내린다**(`cancelMakeupSession`, 상태 `NOSHOW`).
    확정 모달의 `이 근무를 하셨나요?` 라디오와 목록의 *미실시* 버튼 두 곳에서 간다.
    라디오를 둔 이유: 취소하려고 확정 모달에 들어온 사람이 갈 곳이 없으면 **예정 시간을 그대로
    제출해 버린다**(하지도 않은 근무가 수당이 된다).
    - **확정보다 넓게 연다**(`canSelfCancel`) — 확정은 '몇 시간 일했나' 라 근무 뒤에만 뜻이 있지만,
      미실시는 **미리 알수록 좋다**(캘린더에 없는 수업이 남지 않고 확정 요청 DM 도 안 나간다).
      **수당 대상 여부와도 무관하다** — 취소는 돈 이야기가 아니라 '그 일이 있었나' 의 문제다.
      이미 확정한 건만 마감 전까지로 묶는다(급여에 실렸을 수 있다).
    - `CANCELED` 가 아니라 `NOSHOW` 인 이유: 신청은 실제로 있었고 그날 근무만 없었다.
      **지우지 않고 상태만 바꾼다** — 사유가 남아야 관리자가 되돌릴 수 있다.
      확정해 둔 시각은 **지운다**(남기면 되돌렸을 때 하지도 않은 시간이 되살아난다).
  - **관리자가 최종 권한을 갖는다** — `/makeup` 에서 언제든(확정 기간이 지난 뒤에도) 고칠 수 있고,
    고치면 `confirmedBy` 가 ADMIN 으로 덮인다. **신청자가 스스로 확정한 건을 고치면 당사자에게
    알림이 나간다**(`notify` 를 안 켜도) — 본인이 적은 시간과 다른 시간으로 수당이 나가면
    명세서를 받고서야 알게 된다.
  - **수당 대상이 아닌 건에는 신청자 확정 자체를 열지 않는다**(판정은 엔진의 `isPayEligible`
    하나만 쓴다). 확정 화면이 '적은 시간이 곧 수당' 이라는 전제로 쓰여 있어, 결시보강처럼
    반영이 아직 안 정해진 건에 버튼을 두면 직전보강과 똑같이 읽혀 **지급되는 줄 알고 적게 된다**.
    슬랙 목록은 버튼을 빼고 그 줄 밑에 `NOT_PAYABLE_HINT`(왜 닫혔는지 + 관리자 문의)를 작게 붙이고,
    **누른 시점에도 다시 막는다** — 옛 DM 에 남은 버튼은 목록을 안 거치고 바로 눌린다.
    관리자가 *수당 반영* 을 켜서 저장하는 순간 열린다(관리자 화면의 확정 요청 버튼도 그때 켜진다).
  - **직전보강·내신의무보강은 토·일·공휴일에 한 것만 자동 반영**한다(`isPremiumDay`,
    lib/overtime.ts). 평일 보강은 결시보강과 마찬가지로 `payEligible` 로 관리자가 건건이 정한다 —
    평일 근무는 소정근로시간을 조금 넘긴 것일 뿐인 경우가 많고(그 안이면 애초에 대상이 아니다)
    종류만으로 일률 판단하면 실제와 어긋난다. 주말근무(WEEKEND)는 종류 자체가 이미 그 뜻이라
    이 규칙에 걸리지 않는다.
    **판정에 공휴일 표를 반드시 함께 넘긴다**(`isPayEligible(s, policy, holidays)`,
    표는 `holidayYmds()`) — 안 넘기면 공휴일 보강이 평일로 잡혀 **화면(관리자)과 슬랙(직원)이
    다른 답**을 낸다. 모를 때는 평일로 보므로 자동 반영이 아니라 사람이 보게 되는 쪽으로 기운다.
    자정을 넘긴 근무는 **시작한 날**로 판정한다.
  - **확정 요청 알림은 '수당이 반영되는 건' 에만 자동으로** 나간다
    (`autoNotifiesConfirm` = `isPayEligible`. `payEligible` 지정값 우선, 없으면 `categoryDefault`).
    주말근무와 토·일·공휴일 직전·내신보강은 근무 다음날 크론(`runMakeupConfirmReminders`)이 DM 을 보내고,
    **기본 미반영인 건(결시보강·평일 직전·내신보강)은 관리자가 골라서 보낸다**(`POST /api/makeup/[id]/remind`,
    화면의 *확정 요청 보내기*) — 반영하지 않기로 한 건에 확정을 재촉하면 지급되는 줄 알게 된다.
    같은 건에 두 번 보내지 않게 `confirmNotifiedAt` 를 새긴다(관리자가 `force` 로 다시 보낼 수 있다).
    경계는 **다음날 09:00 KST** 에 넘어간다(저장은 KST 벽시계, `now` 는 진짜 UTC) — 의도한 것이다.
    벽시계로 맞춰 자정 직후에 보내면 새벽에 DM 이 울린다(보강은 밤늦게 끝나는 일이 잦다).
  - **내신 상한을 넘겨도 막지 않고 알린다**(`mandatoryCapNotice`) — 상한은 '여기까지 인정한다'
    이지 '더 일하면 안 된다' 가 아니다. 넘긴 분은 엔진이 **수당이 큰 근무부터** 채워 배분하므로
    상한 안에서 근로자에게 가장 유리한 조합이 남는다. 그 사실을 그대로 적는다 —
    초과분이 통째로 사라지는 줄 알면 신고를 줄여 적게 된다.
  산정은 `computeOvertime()`(lib/overtime.ts, 순수 함수) 하나가 전담한다:
  ① 평일 소정근로시간 밖·토요일 → **연장, 가산 없음(×1.0)** ② 일요일·공휴일 → **휴일**(8시간까지 ×1.5, 초과 ×2.0)
  ③ 소정근로시간 안은 대상 아님(월급에 이미 포함) ④ 완전비율제는 제외.
  **연장에 1.5배를 붙이지 않는다** — 이 학원은 주 소정근로가 37.5시간(14~22시·휴게 0.5)이라
  주 40시간 안쪽의 추가근로는 법정 가산 대상이 아니다(법내연장). 가산은 휴일근로에만 붙는다.
  1일 8시간·주 40시간 초과분까지 ×1.5 로 올리려면 `applyStatutoryOvertime` 을 켠다(**기본 꺼짐**,
  켜면 그 주의 늦은 시각부터 승격되고 휴일근로시간은 산입하지 않는다).
  **야간(22~06시) +0.5 가산도 기본 꺼짐**(`countNight`) — 지시가 없으면 붙이지 않는다.
  **소정근로시간은 `Employee.schedule` 에서 뺀다** — 시간표가 비어 있으면 전부 연장으로 잡히므로
  화면이 '근로시간표 없음' 경고를 띄운다.
  **내신의무보강은 내신 기간(`ExamPeriod`)당 상한(기본 10시간)** 까지만 인정하고,
  **수당이 큰 조각부터** 채운다(토 7h·일 7h → 일 7h 전부 + 토 3h). 내신 기간을 등록하지 않으면
  분기(연 4회)로 대신 묶어 상한이 새지 않게 한다. **결시보강은 기본 미반영** — `payEligible` 로 건건이 켠다.
  지급 조건은 `OvertimePolicy`(단일 행) 에 있고 화면(보강·오버타임 → 수당 지급 조건)에서 고친다.
- **오버타임은 급여 산정에 자동으로 실린다.** `runPayrollMonth()` 가 `overtimeInputsFor()` 로
  그 달 확정분을 읽어 `extraHours(법내연장 ×1.0)/overtimeHours(법정연장 ×1.5)/holidayHours/holidayOverHours/nightHours` 를 채운다
  (명시적으로 넘긴 값이 있으면 그쪽 우선). 산정 줄은 `breakdown.overtime` 에 남고
  명세서에 「보강 오버타임 산정 내역서」로 첨부된다(`overtimeDetailHtml`).
  **첨부는 `breakdown.overtime.lines` 가 있을 때만 붙는다** — 즉 보강 신청 → 실근무 확정을 거친 건만이다.
  급여 화면에서 관리자가 시간을 직접 넣은 달은 산정 원장이 없어 붙일 내역서가 없으므로,
  명세서의 '별첨' 문구도 그때만 찍는다(`hasOtDetail`). 조건 없이 찍으면 **없는 별첨을 가리키게 된다**.
  내역서가 없는 달에는 대신 통상시급과 항목별 배수(×1.0/×1.5/×2.0/+0.5)를 적어 스스로 설명되게 한다.
  내신 상한은 **내신 기간 전체** 기준이라 기간이 달을 걸치면 뒤 달 신청이 앞 달 배분을 바꿀 수 있다 —
  발송(SENT)된 달은 재계산하지 않으므로 확정분이 흔들리지는 않는다.
- **공휴일 표는 밖에서 받아 온다**(`lib/holidays.ts` 순수 함수·테스트 있음 + `lib/holiday-service.ts`).
  원본은 한국천문연구원 「특일 정보」(공공데이터포털, `HOLIDAY_API_KEY`).
  **규칙으로 계산하지 않는 이유가 셋**이다: ① 설날·추석·부처님오신날은 **음력**이라 못 구한다
  ② 대체공휴일 규칙이 종류마다 다르다(명절 연휴는 **일요일**과 겹칠 때만, 국경일·어린이날은
  **토요일**과 겹쳐도 생긴다) ③ 지방선거·임시공휴일은 그해에 갑자기 지정돼 어떤 규칙으로도 안 나온다.
  이 표가 하는 일: **휴일근로 가산(§56) · 직전·내신보강 자동 반영 판정 · 시급제 주휴 개근 판정 ·
  연차 분할 기록**. 비면 조용히 틀린다 — 공휴일 근무가 평일로 잡혀 ×1.5 가 안 붙는다.
  (실제로 시드 표에 2026년 한글날·부처님오신날과 대체공휴일 넷이 통째로 빠져 있었다.)
  - **넣기만 하고 지우지 않는다** — 표에는 학원이 직접 넣은 휴무일이 섞여 있을 수 있다.
    목록에 없는 날은 `extra` 로 돌려주고 화면이 보여 준다(지울지는 사람이 정한다).
  - **한 건도 못 받으면 성공으로 보고하지 않는다** — 0건을 '공휴일 없는 해' 로 받아들이면 표가 빈 채로
    조용히 넘어간다. 인증 실패도 **HTTP 200 + 에러 봉투**로 오므로 `responseError()` 로 가른다.
  - 응답 모양이 조용히 바뀐다: 한 건이면 `item` 이 객체, 0건이면 `items` 가 빈 문자열, `locdate` 가
    숫자로도 문자로도 온다. 못 읽은 항목은 버리지 않고 `skipped` 로 센다.
  - **채움 상태를 늘 보여 준다**(`holidayCoverage`) — 올해는 늘, 하반기부터는 내년까지 본다.
    모자라면 설정·보강 화면에 경고가 뜨고, **크론이 모자랄 때만** 받아 온다(평소엔 API 를 안 두드린다).
  - **인증키는 Encoding·Decoding 어느 쪽을 넣어도 된다**(`normalizeServiceKey`). 포털이 두 벌을
    주면서 어느 쪽인지 안 알려 주는데, `URLSearchParams` 가 값을 한 번 인코딩하므로 Encoding 키를
    그대로 넣으면 `%2F` 가 `%252F` 가 되어 **인증이 통째로 실패**한다(겪었다). 키는 base64 라
    `%` 가 들어 있을 수 없어, 있으면 인코딩된 쪽으로 보고 풀어서 쓴다.
  - **JSON 과 XML 을 둘 다 읽는다**(`parseHolidayPayload` → `xmlToEnvelope`). 포털의 데이터포맷
    안내가 XML 이고 `_type=json` 이 안 먹는 때가 있다. XML 은 **JSON 봉투 모양으로 바꿔** 그 뒤로는
    한 길만 타게 한다 — 파서를 두 벌 두면 이름 다듬기·중복 제거가 언젠가 갈라진다.
  - 에러에는 **다음에 뭘 하면 되는지**를 붙인다(`hintFor`) — 포털 코드는 영문 상수라 그대로 띄우면
    무엇을 고쳐야 할지 알 수 없다. 특히 **발급 직후 1시간쯤은 미등록으로 나온다**.
  - 인증키가 없어도 막히지 않는다 — 코드에 든 초기 표(`BUILTIN_HOLIDAYS`, 2025~2027)를
    *초기 표로 채우기* 로 넣거나 화면에서 직접 넣는다. 손으로 적은 표라 **해가 바뀌면 반드시 뒤처진다**.
  - 수동 실행은 `npm run db:holidays-sync`(`YEARS=2026,2027`).
- **경영지원 알림**(`lib/hr-notify.ts` 순수 함수·테스트 있음 + `lib/hr-notify-service.ts`) —
  **계약 종료 예고**와 **생일**을 슬랙으로 보낸다. 받을 부서·시각·며칠 전인지는 `HrNotifySetting`
  (단일 행)에 있고 설정 화면에서 고친다. 기본은 **경영지원 · 60일 전 12:00 · 생일 당일 12:00**.
  발송 시기를 코드가 아니라 설정에 둔 이유: 실무가 바뀌면 배포 없이 고쳐야 하고, 지금 무엇이
  언제 나가게 되어 있는지가 화면에 남아야 한다.
  - **둘의 성질이 정반대라 판정도 다르다.** 계약 만료는 놓치면 계약 공백·무기계약 전환으로
    이어지므로 `'오늘이 딱 N일 전인가'` 가 아니라 **`'남은 기간이 창 안인데 아직 안 알렸는가'`**
    로 본다(`contractsToAnnounce`). 크론이 하루 걸러도, 창 안에서 계약을 새로 만들어도,
    예고 일수를 늘려도 반드시 한 번은 나간다. 대신 **한 계약에 한 번만**(`Contract.expiryNotifiedAt`)
    — 매일 같은 알림이 오면 안 읽게 된다.
    생일은 지나면 뜻이 없으므로 **그날만** 보고 놓친 날을 따라잡지 않는다.
  - **보낸 뒤에 새긴다** — 먼저 새기고 발송이 실패하면 그 계약은 영영 안 알려진다.
    반대는 내일 한 번 더 갈 뿐이라 이쪽이 덜 나쁘다.
  - 기본 경로는 **부서원 각자에게 DM** 이다(채널을 지정하면 그쪽으로). 계약 종료·생일은 그 부서가
    처리할 일이지 전사가 볼 것이 아니다. 한 사람에게 실패해도 나머지는 계속 보낸다.
  - **두 자리 연도(`900810`)에서 나이를 만들지 않는다** — 세기를 알 수 없어 9008년생이 되고
    나이가 음수로 나간다(테스트가 잡았다). 모르면 나이를 비운다.
  - 2월 29일생은 **평년에 2월 28일**로 본다(3월 1일로 미루면 생일이 지난 뒤에 축하하게 된다).
  - 받을 사람이 없거나 슬랙이 연결되지 않았으면 설정 화면이 경고한다(`recipientWarning`) —
    조용히 아무 데도 안 가는 것이 가장 나쁘다.
- **보강은 '보강캘린더' 에 올린다** — 연차 캘린더(`GOOGLE_CALENDAR_ID`)와 **다른 캘린더**
  (`GOOGLE_MAKEUP_CALENDAR_ID`)를 쓰고, 섞이면 안 되므로 없을 때 연차 캘린더로 대체하지 않는다.
  신청·수정 시 동기화하고 취소·미실시면 일정을 내린다(`syncMakeupCalendar`). 캘린더 실패가 신청을 되돌리지 않는다.
- **시급제 주휴는 1주 단위로 판정하되, 근무 형태에 따라 기준이 갈린다**(`lib/timesheet.ts`,
  `WeekSummary.mode`, 테스트 있음).
  - **주5일 이상 계약 = `fixed`** — 근무 요일이 바뀔 이유가 없으므로 **계약서에 정해진 근무요일을
    다 채웠는지(개근)** 로 본다(시행령 §30①). 빠진 요일은 `missingDates` 로 화면에 날짜째 띄운다.
    그 주 공휴일은 채울 날에서 빠지고, 연차 사용일은 출근으로 센다.
    주휴시간 = min(계약 주 소정근로시간 ÷ 5, 8) — 실근로가 많아도 늘지 않는다.
  - **주2~4일 계약 = `actual`** — 계약서의 주당 근로일은 문서화용 형식일 뿐이다. **매월 말에 학원이
    다음 달 근로계획표를 짜서 공지하는 것이 그 달의 소정근로**이고 달마다·주마다 바뀐다.
    그래서 계약 요일로 개근을 따질 수 없고, 4주 평균을 내면 특정 주에 많이 일한 사실이 묻힌다.
    **그 주 실근로 15시간 이상이면 그 주에 한해** 발생하고 다른 주가 미달이어도 무관하다.
    계획표대로 나온 결과가 실근로이므로 실근로 충족이 개근을 겸한다.
    주휴시간 = min(그 주 근로시간 ÷ 5, 8).
  - 모드 선택은 `plan.days >= 5 && plan.hours >= 15` — 주5일인데 계약 소정이 15시간 미만이면
    애초에 §18③ 초단시간이라 실근로 판정으로 넘긴다.
  - 공통: **그 주(월~일) 내내 근로관계 존속**해야 한다. 주휴일 전 퇴사·주 중간 입사면 미발생.
    2021.8.4. 행정해석 변경(임금근로시간과-1736)을 따른다 — 주휴일까지만 재직하면 8일째 근로관계가
    없어도 발생한다. **'퇴사 주는 무조건 미지급' 은 옛 해석이라 그대로 구현하면 체불**이 된다.
  - 공통: **휴게 30분은 유급·무급(`breakPaid`)과 무관하게 항상 빼고 15시간을 잰다** —
    휴게가 유급이라는 건 '그 시간도 돈을 준다' 는 뜻이지 '그 시간이 소정근로시간이다' 는 뜻이 아니다.
    그래서 **판정에 쓰는 `actualHours` 는 급여 산정시간(`paidHours`)과 다를 수 있다**(체류 15h = 판정 13.5h).
  - 달을 걸친 주는 **주휴일(일요일)이 속한 달**에서 한 번만 센다(이중 지급·누락 방지).
  - 계약 근로시간표는 `fixed` 의 근무요일·주휴시간 산정과 **연차일의 유급 시간 환산**에 쓴다.
- **시급제에는 연장·휴일 가산을 붙이지 않는다.** 15시간을 넘긴 주마다 주휴를 인정해 주는 것이
  초과근로분을 갈음해 온 구조이고, 1일 4.5시간 내외·주 20시간 안쪽이라 법정 가산 요건
  (1일 8시간·주 40시간 초과)에 애초에 닿지 않는다. **보강 오버타임도 시급제는 제외**한다
  (`excludedByScheme`, lib/makeup-service.ts) — 보강 시간이 이미 출퇴근 기록에 잡혀 실근로시간으로
  지급되므로 여기서 또 얹으면 **같은 시간을 두 번 주는 셈**이다. 보강 신청·캘린더 등록은 그대로 한다.
- **일별 기록은 DB(`TimesheetDay`)에 남긴다** — 달을 걸친 주를 뒤 달에서 지급하려면 그 주 **앞부분(앞달
  날짜)의 근로시간**을 알아야 15시간을 판정할 수 있는데, 뒤 달 기록표에는 그 며칠이 없다.
  저장을 안 하던 때는 앞달 며칠이 0으로 깔려 **앞달에도 뒤 달에도 안 붙어 그 주 주휴가 통째로 사라졌다**.
  업로드할 때마다 `upsert` 로 쌓고(같은 날 다시 올리면 **파일이 이긴다**), 계산 전에 앞뒤 ±14일을 읽어 합친다.
  **한 번도 올라온 적 없는 날은 '결근' 이 아니라 '기록 없음'** 이다(`knownFrom` = 그 직원의 최초 저장일).
  그런 날이 낀 주는 `partial` 로 표시하고 **판정을 보류**한다 — 모르는 날을 0으로 깔면 15시간에 못 미쳐
  주휴가 부당하게 빠지므로, 그 주가 든 앞달 기록표를 한 번 올리면 자동으로 확정된다.
  `attendedDates` 로 어느 날 나왔는지 화면에 띄운다.
- **기록표 파싱은 조용히 실패하지 않는다**(`parseHourCell`/`parseDateCell`, 테스트 있음) —
  근무시간 열은 보통 `퇴근−출근` 수식이라 엑셀 시간값(하루=1)이지만 사람이 고친 칸은 그냥 숫자(`5`)나
  문자(`"5:00"`)로 들어온다. 예전엔 그런 칸을 통째로 버려서 **그날 근로시간이 통째로 빠지고
  그 주 15시간 판정이 틀어졌다**.
  이제 숫자·문자·Date 를 모두 읽고, 그래도 못 읽은 행은 `skipped` 로 세어 화면에 빨갛게 알린다.
  날짜 칸도 `7/20`·`7월 20일` 같은 표기를 받고(연도는 같은 표의 온전한 날짜에서 빌린다),
  2000년 미만으로 튀는 작은 숫자는 날짜가 아닌 것으로 본다.
- **시급제 시간은 셋으로 나눠 다룬다**(`MonthlyTimesheetResult`) — `stayHours`(학원 체류, 출퇴근 기록
  그대로) · `breakHours`(근무일마다 30분) · `netHours`(순 근로 = 체류 − 휴게).
  급여 산정시간 `paidHours` = (`breakPaid` 면 체류, 아니면 순 근로) **+ 연차 유급시간**.
  **연차를 쓴 날은 1일 소정근로시간(주 소정 ÷ 주 근무일수)을 채운 것으로 본다** — 연차휴가는
  유급휴가(§60)이므로 기록표가 비어도 그만큼 지급하고 주휴 15시간 판정에도 넣는다(반차는 0.5일 비례).
  연차 미적용 직원은 유급 인정에서 빠진다.
  이 구분은 계산에 쓰이지 않는 `breakdown.timesheet` 로도 실려 가 **명세서가 체류/휴게/순 근로/연차를
  나눠 보여 준다** — 출퇴근 기록과 급여 시간이 왜 다른지 직원이 명세서만 보고 알 수 있어야 한다.
- **기록표도 직접 입력도 없이 산정한 시급제 달은 급여 화면에서 지급액을 붉게 그린다**
  (`isEstimatedHourly`, lib/payroll.ts. 테스트 있음). 그 달은 계약 근로시간표로 **만근을 가정해**
  어림잡은 금액이라 결근·지각이 하나도 반영되지 않는다 — **실제보다 많이 나오는 쪽으로 틀리므로**
  그대로 이체하면 과지급이 된다. 숫자만 붉히지 않고 `추정 · 기록표 미반영` 을 밑에 작게 적는다
  (색만으로는 무엇이 잘못됐는지 알 수 없다).
  **판정은 엔진이 쓰는 함수를 화면이 그대로 가져다 쓴다** — 화면이 따로 판정하면 언젠가 갈라져
  추정인데 검게 나오거나 실제 기록이 있는데 붉게 나온다. `computePayroll` 의 '시간기록표 미반영'
  안내문과 답이 같은지 테스트로 못박아 둔다.
  **0시간도 추정으로 본다** — 0을 '실제로 0시간 일했다' 로 읽으면 기록이 없는 달이 검게 나간다.
- 4대보험/세율은 하드코딩 금지 — `InsuranceRate`(설정 화면에서 수정). 세액표는 `TaxBracket`.
- 계산식 변경 시 `lib/*.test.ts` 를 먼저 갱신하고 `npm test` 로 검증.
- 관리자 로그인은 비밀번호 공유(`ADMIN_PASSWORD`) 방식이라 화면 작업의 '누가' 는 남지 않는다.
  슬랙 경유 작업만 사용자까지 기록된다. 개인 구분이 필요해지면 계정 모델을 도입해야 한다.
- **Supabase 의 `public` 스키마는 공개 API(PostgREST)로 자동 노출된다 — RLS 로 막아 둔다.**
  `https://<ref>.supabase.co/rest/v1/...` 는 `anon` 키만 있으면 열리고, 그 키는 원래
  브라우저에 심으라고 주는 **공개 키**다. RLS 가 꺼져 있으면 그 키를 가진 누구나 전 직원의
  주민번호·급여·계좌번호를 읽고 고치고 지울 수 있다(Supabase 점검의 `rls_disabled_in_public`).
  이 앱은 supabase-js 를 안 쓰고 **Prisma 로 직접 접속**하므로 anon 키를 쓸 일이 없다 — 문은 닫는다.
  `scripts/db-rls.mjs` 가 `public` 의 모든 테이블에 **정책 없이 RLS 만** 켠다:
  **테이블 주인은 RLS 를 통과하므로**(Postgres 기본. `FORCE ROW LEVEL SECURITY` 를 걸어야 주인도 막힌다)
  테이블을 만든 역할로 접속하는 앱은 그대로 되고 anon·authenticated 만 전부 거부된다.
  **`prisma db push` 는 RLS 를 모른다** — 모델을 추가하면 그 테이블은 다시 무방비가 되므로
  `db-deploy.mjs` 가 배포 때마다 훑는다(멱등). **push 가 실패해도 이건 돌린다** —
  보안 잠금이 스키마 동기화 성공에 매달리면 그 사이 데이터가 공개된 채로 남는다.
  접속 역할이 테이블 주인도 아니고 `bypassrls` 도 없으면 **아무것도 하지 않고 멈춘다**
  (그대로 켜면 앱이 자기 데이터를 못 읽는다). 수동 실행은 `npm run db:rls`.
- **개인정보(주민번호·계좌번호)는 DB 에 암호문으로 넣는다**(`lib/crypto.ts`, 테스트 있음).
  AES-256-GCM, **값마다 IV 를 새로 뽑아** 같은 주민번호라도 암호문이 매번 다르고,
  인증 태그로 위변조를 잡는다. 저장 형태는 `enc:v1:<iv>:<tag>:<ct>` (base64).
  키(`ENCRYPTION_KEY`, 32바이트)는 **앱 환경변수에만** 둔다 — DB 안에 두면 DB 를 얻은 사람이
  키도 같이 얻어 암호화가 무의미해진다.
  - **암·복호화는 DB 경계 한곳에서 한다**(`lib/db.ts` 의 Prisma 확장). 부르는 쪽은 평문을
    쓰고 평문을 받는다. 읽기는 `result` 확장이라 **중첩 include 에도 적용된다**
    (`payrollRecord.findMany({include:{employee:true}})` 로 딸려 나온 직원도 풀린다 — 실측 확인).
    쓰기는 `query` 확장이 create/update/upsert/createMany/updateMany 의 data 를 훑는다.
    한곳에 모은 이유: 읽는 자리를 빠뜨리면 화면에 암호문이 뜨고(눈에 띈다),
    **쓰는 자리를 빠뜨리면 평문이 조용히 저장된다**(이쪽이 훨씬 나쁘다).
  - **`enc:v1:` 접두어가 없으면 아직 안 옮긴 평문으로 보고 그대로 돌려준다** — 배포와 데이터
    이전 사이에 두 형태가 섞여 있어도 화면이 깨지지 않는다. 옛 데이터는
    `npm run db:encrypt-pii` 로 한 번 훑는다(미리보기 기본, 실제 반영은 `APPLY=1`).
    이미 암호화된 값은 건너뛰므로 멱등하고, **암호화 직후 되돌려 원문과 대조**한 뒤에만 쓴다
    (못 읽는 값으로 덮어쓰지 않는다). 로그에도 값을 가려서 남긴다.
  - **키가 없으면 평문으로 저장하고 경고만 남긴다** — 여기서 던지면 키를 안 넣은 배포에서
    직원 등록이 통째로 막힌다. 대신 설정 화면에 빨간 경고를 띄워 조용히 지나가지 않게 한다.
  - **복호화에 실패하면(키가 다르거나 값이 손상됨) `null` 을 돌려준다** — 깨진 문자열을 흘리면
    그게 주민번호인 줄 알고 세무 시트에 실린다. 빈 값이면 세무 시트·이체 파일이 이미
    "비어 있다" 고 경고하므로 사람이 알아챈다.
  - **검색은 못 한다** — IV 가 매번 달라 `where: { rrn: … }` 가 성립하지 않는다.
    지금 코드에 그런 조회가 없어서 이 방식을 골랐다. 필요해지면 결정적 해시 열(blind index)을 따로 둔다.
  - ⚠️ **키를 잃으면 복구할 수 없다.** 세무 신고에 쓰는 주민번호도 같이 잃는 것이다.
    키를 바꾸려면 옛 키로 풀어서 새 키로 다시 감싸야 한다.
  - 암호문 자체를 봐야 하는 작업(이전 스크립트)은 확장을 거치지 않는 `prismaRaw` 를 쓴다.
- `.env`, `*.db`, `storage/` 는 커밋 금지(.gitignore).

- **세무사무소 제출자료**(`lib/payroll-export.ts`, 테스트 있음) — 급여 산정·수동 조정이 끝난 뒤
  급여 화면의 *📊 세무사무소 제출자료* 로 엑셀을 받는다(`GET /api/payroll/export`).
  세무대행 업체에 넘기던 시트를 그대로 재현하므로 **열 관계가 맞아떨어져야 한다**:
  `세전총계 = 세전급여 + 인센티브 + 오버타임 + 미사용연차 + 상여`,
  `입금액 = 세전총계 − 공제 − 인센티브유보액 − 주차비 − 실비 − 기타공제`.
  **인센티브는 세전급여에 섞지 않고 열을 따로 둔다** — 세무사가 고정급과 성과급을 갈라 봐야 하고,
  뒤쪽 '인센티브유보액'(인센티브 × 1/12)과 대조하려면 원천액이 시트에 있어야 한다.
  **'오버타임수당' 열에는 포괄임금 약정분을 넣지 않는다**(`variableOvertimeOf`) —
  레코드의 `overtimeP`·`nightP` 에는 계약서 제4조의 고정 시간외·야간이 섞여 있어 그대로 쓰면
  매달 오버타임이 발생한 것처럼 보인다. 그래서 금액이 아니라 **그 달 입력·확정된 시간**
  (수기 입력분 + 보강·오버타임 실근무 확정분)에서 배수대로 다시 세운다. 빠진 약정분은 세전급여로
  가므로 세전총계는 그대로고, 포괄임금 계약자는 **세전급여가 계약 월 급여총액과 정확히 맞는다**.
  **'공제' 열은 법정공제(4대보험·소득세·지방세)만** 담는다 — 유보금·주차비를 여기 섞으면
  세무사가 신고할 원천징수액이 틀어진다. 실비·기타공제는 평소 0 이라 원래 시트에 열이 없어서,
  **값이 있는 달에만 열을 붙인다**(조용히 빼면 입금액이 안 맞는다).
  행마다 `reconcileRow()` 로 합계를 검산하고, 어긋나거나 **주민번호가 빈 직원**이 있으면
  시트 끝과 화면 경고로 알린다(빈칸으로 넘어가면 세무사가 신고를 못 한다).
  지급일은 익월 `Company.payday` 일(계약서 제4조와 같은 규칙).

- **은행 대량이체 파일**(`lib/bank-transfer.ts`, 테스트 있음) — 세후 금액이 확정된 뒤 급여 화면의
  *🏦 은행 이체 파일* 로 받아 인터넷뱅킹 '파일이체' 에 그대로 올린다(`GET /api/payroll/bank-export`).
  하나은행 대량이체 양식이라 열 9개(`입금은행|입금계좌번호|입금액|예상예금주|입금통장표시|출금통장표시|메모|CMS코드|받는분 휴대폰번호`)와
  순서를 손대지 않는다 — 양식 안내문이 **"Excel Sheet 의 구조는 절대로 변경하지 마십시오"** 라고 못박고 있다.
  파일은 **.xls(BIFF8)** 로 낸다(안내문이 xls/csv/txt 만 받는다). 계좌번호는 **문자 셀**로 써야 한다 —
  숫자로 두면 앞자리 0 이 날아가고 지수 표기가 된다.
  - **한 사람이 두 줄이 될 수 있다** — 퇴직유보금은 급여와 섞지 않고 별도 통장으로 보내므로
    (확인서 제6조) 유보금 줄이 따로 붙는다. `Employee.retentionBank`/`retentionAccount` 가 비면 급여 계좌로 간다.
    세무 시트의 `입금액(공제후)` 은 이미 유보액을 뺀 금액이라 두 줄을 더하면 맞아떨어진다.
  - **입금통장표시의 소득 구분은 위탁계약 여부로 가른다**(`incomeLabelOf`) — 위탁만 `유쌤_7월사업소득`,
    나머지는 `유쌤_7월급여`. 세무구분(3.3%)으로 가르지 않는다. 3.3% 로 신고해도 실질이 근로자면
    본인이 받는 돈은 급여이고, 통장에 '사업소득' 으로 찍히면 근로자성을 스스로 부정하는 기록이 남는다.
    앞의 약칭은 `Company.transferAlias`(설정 화면) — 상호를 그대로 쓰면 통장 적요 자리가 넘친다.
  - **계좌가 비면 줄을 만들지 않는다** — 빈칸으로 올리면 그 건만이 아니라 파일 전체가 반려된다.
    대신 누가 빠졌는지 화면에 띄운다. 지급액 0원도 건너뛴다.
  - 순서는 **사업소득(위탁) → 급여 → 퇴직유보금**. 은행 화면에서 위에서부터 눈으로 훑는 순서다.
- **계좌 일괄 등록** — 같은 양식을 거꾸로 읽어 직원 정보의 은행·계좌를 채운다
  (`parseBankAccountWorkbook`/`planBankAccounts`, `POST /api/employees/bank-import`,
  화면: 직원 관리 → *명단 일괄 등록* → **계좌 등록** 탭). 매달 손으로 채워 쓰던 이체 파일에 이미
  전 직원 계좌가 들어 있어 그 파일을 그대로 올리면 된다. **예상예금주** 로 직원을 찾고,
  입금통장표시가 '퇴직유보금…' 인 줄은 유보금 전용 통장으로 넣는다(뭉뚱그리면 급여 계좌를 덮어쓴다).
  **비어 있는 항목만** 채우는 게 기본이다 — 계좌를 잘못 덮어쓰면 돈이 남에게 간다.
  은행명은 `normalizeBankName()` 이 "국민"/"KB" → "국민은행" 으로 맞추되 **표에 없는 은행은 손대지 않는다**.

- **입사 서류는 부서가 정한다**(`lib/departments.ts` + `Department` 모델, 테스트 있음).
  부서를 코드 상수가 아니라 **데이터**로 둔 이유는 부서마다 받아야 할 서류가 다르기 때문이다 —
  상수로 두면 부서가 늘 때마다 배포해야 하고, 새 부서에 무슨 서류가 나가야 하는지 아무도 정하지
  않은 채로 남는다. **설정 → 부서 관리에서 부서를 추가할 때 서류 정책을 함께 묻는다**
  (`DEPT_DOC_FIELDS`: 복무서약서-II · 프로필 홍보 동의서 · 건강서약서 · 경업금지 조항).
  - **부서가 비었거나 등록되지 않은 이름이면 신규입사 패키지 발급을 막는다**(`documentBlockReason`).
    기본값을 지어내면 조교에게 강사용 서약서가 나가거나 그 반대가 된다 — 사람이 서명한 뒤에야 드러난다.
  - **보안서약서는 전 직원이 쓰고 경업금지 조항(퇴사 후 6개월·반경 2km)만 부서로 가른다.**
    직업선택의 자유를 제한하는 조항이라 원생 관계를 실제로 쥐는 자리에만 붙이는 것이 법적으로도 안전하다.
  - 부서 **이름을 바꾸면 그 부서 직원의 department 문자열도 함께 옮긴다** — 안 옮기면
    '등록되지 않은 부서' 가 되어 그 사람들 발급이 통째로 막힌다.
  - 쓰지 않을 부서는 **지우지 말고 끈다**(`active=false`). 소속 직원이 있으면 삭제가 거부된다.
- **계약서 세트(`contractBodies`)에는 그 계약의 성질상 함께 받아야 하는 확인서가 붙는다** —
  인센티브·비율제면 **퇴직유보금 확인서**, 위탁계약이면 **개인사업자 지위 확인서**.
  **갱신계약에도 그대로 붙는다**: 신규 때는 해당이 없다가 갱신하며 조건이 바뀌는 일이 있는데,
  그때 확인서를 안 챙기면 근거 없이 유보금을 떼거나 위탁으로 처리하게 된다.
  판정은 **계약의 templateKey 를 먼저 보고** 직원 카드는 그다음이다(계약이 진실).
  서약서·동의서는 계약서 세트에 넣지 않는다 — 갱신 때마다 다시 받을 서류가 아니다.
- **직장 내 괴롭힘 근절 서약서는 법정 의무가 아니다**(근로기준법 §76의2·3 은 금지·조사·조치
  의무만 정하고 서약서 징구를 요구하지 않는다). 복무서약서 8항과도 겹친다. 그럼에도 넣은 이유는
  ① 지금까지 실제로 받아 온 서류이고 ② 분쟁 시 예방 노력의 증빙이 되기 때문이다(2026-08 결정).
- **근로계약서 제7조는 계약기간이 있느냐로 갈린다**(`ct.endDate` 하나로 판정, 테스트 있음).
  - **기간제** → 「계약의 종료 및 의원사직」. **만료로 종료함이 원칙**이고 중도해지는 민법 §661 의
    부득이한 사유 또는 당사자 합의가 있을 때에 한한다(`fixedTermEndClauses()`). 합의 종료일은
    학사일정을 고려해 **정규·내신 종료일**을 원칙으로 한다 — 학원은 학기 중간에 사람을 뺄 수 없다.
    통보는 **퇴직 희망일 2개월 전 서면**이고 **부서와 무관하다**(`FIXED_TERM_NOTICE_LABEL`) —
    부서로 갈리는 `resignNoticeDays()`(교수부 60일 / 그 외 30일)는 **기간의 정함이 없는 계약**의
    의원사직에만 쓴다. 둘을 한 함수로 합치지 말 것: 성질이 다르다(만료가 원칙인 계약의 예외적
    중도해지 ↔ 언제든 가능한 사직).
  - **기간의 정함이 없는 계약** → 예전 그대로 「의원사직」. 만료가 없으니 만료 조항도 없다.
  - 조항 번호는 `CIRC[]` 로 이어 붙이므로 앞 조항이 늘면 뒤가 저절로 밀린다 — 번호를 손으로 적지 않는다.
  - **강의위탁계약서(비율제)는 건드리지 않았다** — 민법 §661 은 「고용」 규정이라 위임·도급 성질의
    위탁계약에 그대로 인용할 수 없다(그쪽은 제3조 ④ 의 자체 통보 조항을 쓴다).
- **개인정보 국외 처리는 '동의' 가 아니라 '고지' 다**(`OVERSEAS_PROCESSORS` / `overseasNoticeBox()`,
  개인정보 수집·이용·제공 동의서에 붙는다. 테스트 있음).
  개인정보 보호법 §28의8①3호는 **계약의 체결·이행에 필요한 처리위탁·보관**이면 §28의8② 의 사항을
  알리는 것으로 국외 이전을 허용한다. 동의를 근거로 삼으면 직원이 철회했을 때 그 사람만 슬랙·캘린더에서
  빼야 하는데 그건 실무가 돌아가지 않는다. 그래서 **고지받았다는 확인**만 받는다.
  - **실제로 개인정보를 보내는 곳만 적는다.** 새 외부 연동을 붙이면 **여기에 함께 적어야 한다** —
    빠뜨리면 고지 의무를 못 지키고, 안 보내는 곳을 적으면 사실과 다른 고지가 된다.
  - **'국외 이전' 과 '국내 보관' 을 갈라 적는다**(`OVERSEAS_PROCESSORS` / `DOMESTIC_REGION_PROCESSORS`).
    앱은 **서울 리전**(`vercel.json` 의 `regions: ["icn1"]`), DB 도 **서울 리전**(Supabase
    `ap-northeast-2`)이라 **데이터가 국외로 나가지 않는다** — 이 둘을 국외 이전으로 적으면 사실과
    다르다. 대신 수탁자가 미국 법인이라 **유지보수 목적의 국외 접근 가능성**을 함께 알린다(§26 위탁 공개).
    실제로 국외 서버로 나가는 것은 **슬랙·구글 캘린더**뿐이다.
    ⚠ **리전을 바꾸면 이 목록도 고쳐야 한다** — 문서만 남고 실제가 달라지면 사실과 다른 고지가 된다.
  - ⚠ **생성형 AI(Claude·ChatGPT)는 목록에 없다.** 이 앱은 어떤 AI API 도 호출하지 않는다 —
    코드를 사람이 AI 의 도움으로 작성한 것과, 앱이 직원 정보를 AI 에 보내는 것은 다른 이야기다.
    다만 **담당자가 직원 정보를 직접 AI 서비스에 붙여넣는다면** 그건 별도의 국외 이전이므로
    그때는 목록에 추가하고 이용 방침을 따로 정해야 한다.
  - 앱이 연동해 보내는 것(슬랙·구글 캘린더)만이 아니라 **직원이 업무에 쓰는 도구**(구글 드라이브·노션)도
    적는다 — 회사가 위탁한 곳에 개인정보가 있으면 고지 의무에는 차이가 없다. 그래서 **새 연동을
    붙일 때뿐 아니라 회사가 새 업무 도구를 도입할 때도** 목록을 고쳐야 한다.
  - 상자에 `avoid` 를 건다 — 쪽 경계에 걸리면 테두리가 두 쪽으로 잘려 한 항목으로 안 읽힌다.
    이 항목이 붙으면서 동의서는 **두 장**이 된다(신규입사 패키지 11장 → 12장). 계약서는 2장 그대로다.
- **임금공제 동의서는 3.3% 대상자에게 4대보험을 적지 않는다**(`deductsInsurance`, 테스트 있음).
  판정은 **급여 엔진과 같아야 한다** — 엔진은 위탁계약자면 세무구분이 EMPLOYEE 로 남아 있어도
  보험료를 떼지 않고 3.3% 로 처리한다. 문서만 4대보험을 적으면 **떼지도 않는 것을 동의받는 셈**이고,
  위탁계약자에게는 4대보험 가입 사실을 시인하는 것처럼 읽혀 근로자성 다툼의 빌미가 된다.
  공제 사유(사회보험료 납부 ↔ 소득세법 §127 원천징수)와 각주(근로기준법 §43 은 근로자에게만 뜻이 있다),
  '임금 ↔ 지급액' 표현도 함께 갈린다.
- **계약서가 넘치면 글자를 줄여 2장에 담는다**(`PdfOptions.fitPages`, `bilateralPdfOpts` 가 `2` 를 준다).
  근로계약서는 2장에 맞춰 놓은 서식이라 **조항이 하나 늘 때마다 서명란만 3장째로 밀려
  거의 빈 장이 한 장 붙는다**(기간제 계약종료 조항을 넣으면서 실제로 겪었다).
  조항이 늘고 주는 것은 앞으로도 계속 있을 일이라 그때그때 문구를 줄이는 대신,
  **뽑아 보고 안 맞으면 한 단계 줄여 다시 뽑는다**(장수는 렌더 전에 알 수 없어 이 방법뿐이다.
  간인·쪽번호가 이미 같은 이유로 두 번 뽑고 있다). 맞는 순간 멈추므로 대개 첫 판에서 끝난다.
  - 배율은 `zoom` 으로 준다 — **여백·표·선까지 함께** 줄어야 조판이 유지된다.
    `font-size` 만 줄이면 pt 로 못박은 제목·표가 그대로 남아 균형이 깨진다.
  - **간인 layer 는 zoom 밖에 둔다**(`<div class="fit-scale">` 로 본문만 감싼다) — 간인은 종이 기준
    절대 mm 좌표라 함께 줄면 장 경계에서 어긋난다. 축소본과 원본의 간인 좌표가 같은지 실측 확인했다.
  - `.doc-page` 의 `min-height` 도 zoom 을 받으므로 **배율만큼 키워 상쇄**한다 — 안 그러면 한 장
    높이가 같이 줄어 서명란이 종이 중간에 뜬다(`margin-top:auto` 로 바닥에 붙이는 구조라서).
  - `FIT_SCALES` 는 0.86 에서 멈춘다(10pt → 8.6pt). 끝까지 줄여도 안 맞으면 가장 작은 판을 쓴다 —
    더 줄이면 읽을 수 없다.
- **문서 조판에서 되풀이해 겪은 것 셋** (`lib/pdf.ts` 의 CSS, `lib/fonts.ts`).
  - **NanumGothic 서브셋에 없는 글자는 서버리스에서 빈칸으로 빠진다.** 로컬은 시스템 폰트로
    대체돼 멀쩡해 보여서 배포 후에야 드러난다. KS 심볼(①※○)은 `SYMBOL_RANGE` 로,
    **낫표(「」)는 `PUNCT_RANGE`** 로 보강한다 — 법령 인용에 쓰는데 빠지면 그 자리만큼
    문장이 벌어져 보인다. 새 기호를 쓰기 전에 `fontTools` 로 cmap 을 확인할 것.
  - **번호 항목 아래 하위 항목은 `.sub` 가 아니라 `.sub-item`** — `.sub` 는 매달린 들여쓰기라
    부모 번호와 같은 줄에서 시작해 몇 번에 딸린 것인지 안 읽힌다.
  - **서명란(`.doc-foot`)에 `break-before:avoid`** — `margin-top:auto` 로 바닥에 붙이는 구조라
    본문이 한 장을 살짝 넘기면 서명란만 덩그러니 다음 장으로 간다. 조항이 많은 서약서는
    `.compact` 로 조판해 한 장에 담는다.
- **쪽번호·이니셜란·간인은 「한 건의 서류」 안에서만 뜻이 있다** — 여러 서류를 한 파일로 묶는다고
  통째로 이어 매기면 안 된다(신규입사 패키지 12장짜리 `1 / 12`). 그래서 **서류마다 따로 렌더해
  PDF 로 합친다**(`DocGroup` / `docGroupsToPdf` / `mergePdfs`, lib/pdf.ts. pdf-lib).
  브라우저는 묶음마다 새로 띄우지 않고 한 번만 띄운다 — 서버리스에서 문서 수만큼 곱해지면 감당이 안 된다.
  - **양자가 서명하는 서류(계약서·별지)에만** 조판 옵션을 준다. 서약서·동의서·확인서는
    **을이 회사에 내는 일방 서류라 갑의 서명란이 자체가 없다** → 각 장 이니셜란도, 쪽번호도 붙이지 않는다.
  - **근로계약서와 별지(인센티브 산정 계약서)는 각각 다른 묶음**이다. 한 묶음으로 두면 계약서
    마지막 장에도 이니셜란이 붙고 쪽번호가 별지까지 이어져 `2 / 3` 이 된다.
  - **마지막 장에는 이니셜란을 넣지 않는다** — 그 장에는 이미 양자 서명란이 통째로 있어 같은 자리에
    약식 서명을 또 받을 이유가 없다. 넣은 판과 뺀 판을 각각 뽑아 장별로 골라 붙인다(`pickPages`).
  - 계약서는 **단독 발급이든 패키지든 같은 모양**으로 나온다(`bilateralPdfOpts`) — 경로에 따라
    간인이 있고 없고가 달라지면 같은 계약서로 안 보인다.
- **만든 PDF 를 여는 창은 `await` 뒤에서 열지 않는다**(`lib/open-pdf.ts`, 화면은 이것만 쓴다).
  `window.open` 은 **클릭과 같은 실행 흐름**에서 불러야 한다 — `await` 를 하나라도 거치면
  사용자 제스처가 풀려 **팝업 차단에 조용히 걸린다**(오류도 안 뜨고 탭도 안 열려서
  "눌러도 아무 일이 없다" 가 된다). 크롬은 클릭 후 **약 5초까지만** 봐주므로
  (transient user activation) **빠른 문서는 열리고 오래 걸리는 것만 안 열린다** — 이게 고약하다.
  실제로 재직증명서(1장)는 멀쩡한데 **신규입사 패키지(서류 4종·11장)만** 안 열렸다.
  그래서 순서를 뒤집는다: **누르는 순간 빈 탭을 먼저 띄우고**(안내문을 그려 둔다) 다 만들어지면
  그 탭을 옮긴다. 실패하면 그 탭을 **반드시 닫는다**(안내문만 남으면 계속 기다리게 된다).
  팝업이 아예 막힌 브라우저면 **내려받기로 떨어뜨린다**.
  ⚠ **playwright 기본값은 `--disable-popup-blocking` 이라 테스트에서 드러나지 않는다** —
  확인하려면 `ignoreDefaultArgs: ["--disable-popup-blocking"]` 로 띄우고 응답을 5초 넘게 늦춰야 한다.
- **앱 버전을 화면에 띄운다**(`lib/version.ts` → 사이드바 하단 `v1.1.0 · a1b2c3d`).
  이게 없으면 "고쳐 올렸는데 화면이 그대로다" 일 때 배포가 안 된 건지 코드가 잘못된 건지
  가릴 수가 없다 — 실제로 겪었다(DB 는 암호화됐는데 앱은 옛 코드라 암호문이 보였다).
  기능을 더하면 `package.json` 의 minor 를, 고치면 patch 를 올린다.

## 자주 하는 작업
- **계약서 문구 수정** → `lib/documents.ts` 의 `contractHtml`.
- **법인 인감 등록·교체** → 설정 → *법인 인감(직인)* 에 누끼(배경 제거)된 PNG 를 올린다.
  크기·위치를 바꾸려면 `lib/pdf.ts` 의 `.stamp-anchor .stamp-img`.
- **직원 보수조건 변경** → 직원 상세 → 계약 이력 → *조건 수정*(오타 정정) 또는 *신규 계약 작성*(변경 발효일 지정).
- **포괄임금 약정시간 입력** → 계약 폼(신규 계약 작성 / 조건 수정)의 *포괄임금 약정시간* 접이식 영역.
  시간만 넣으면 기본급·시간외·야간 금액이 자동 분해되어 계약서 제4조와 명세서에 함께 반영된다.
- **급여 항목 추가** → `lib/payroll.ts`(계산) + `schema.prisma`(PayrollRecord) + `lib/documents-pay.ts`(명세서 표시).
- **연차 규칙 변경** → `lib/leave.ts`(`annualLeaveDays`, `generateGrants`) + 테스트.
- **직원별 연차 내역 보기** → `/leave/[id]` (연차 화면에서 이름 클릭). 사용·반영 내역을
  "7월 29일 (수) · 연차 1일 사용" 처럼 한 줄씩, 슬랙 신청 이력, 발생 lot 이력을 함께 보여준다.
- **그날 누가 쉬는지 보기** → `/leave` 의 *달력 보기* 탭. 대시보드에도 *1주일 내 휴가·휴무* 가
  승인 대기 옆에 뜬다. 색은 연차/대휴/병가·경조/휴무로 갈리고 **승인 대기는 노랑 ⚠** 이다.
- **평일 휴무 가져오기** → `/leave` 의 *휴무 가져오기* (구글 연차 캘린더의 `(휴무)홍길동`).
  크론이 매시 자동으로도 돈다. **연차가 아니라 잔여를 깎지 않는다.**
  이름을 못 찾은 일정은 버튼 밑에 경고로 뜬다 — 퇴사자이거나 캘린더 제목 오타다.
- **쓰던 연차 관리시트 가져오기** → `/leave` 의 *연차 시트 가져오기* (`lib/leave-import.ts` 파서·계획,
  `/api/leave/import`). **사용 내역만** 가져오고 발생일수는 시스템 계산을 쓴다(시트=회계연도, 시스템=입사일 기준).
  휴가종류 가중치는 시트가 자체 계산한 누적사용일수와 대조해 확정 — 연차 1일·반차 0.5일만 연차를 깎고
  대체휴일은 대휴(COMP), 공가공상·무급휴가는 차감하지 않는다. 같은 날짜 사용분은 다시 넣지 않아 재실행이 안전하다.
- **직원 명단 일괄 등록** → `lib/employee-import.ts`(엑셀 파서·헤더 alias·값 정규화, 테스트 있음)
  + `/api/employees/import`(GET=빈 양식, POST=미리보기/등록). 등록 시 초기 계약도 함께 만든다.
- **이미 등록된 직원의 빈 인적사항 채우기** → 같은 화면의 *정보 채우기* 탭
  (`parseEmployeeWorkbook(buf, {mode:"fill"})` → `planFill()` → `mode=fill-preview|fill-commit`).
  사번→성명 순으로 직원을 찾고, **비어 있는 항목만** 채운다(덮어쓰기는 선택). 채울 수 있는 건
  `FILL_FIELDS`(인적사항)뿐 — 보수조건은 계약이 정하므로 이 경로로 바뀌지 않는다.
  파일에 **없는 열은 건드리지 않는다**(`presentKeys`) — 파서 기본값(부양가족수 1 등)이 새어 들어가면 안 된다.
  주민번호는 `normalizeRRN()` 으로 형식을 통일하고 생년월일을 자동 도출, 화면·이력에는 마스킹해 남긴다.
- **보강·주말근무 실근무 확인** → 신청자가 슬랙 「내 신청 내역」에서 근무 다음날 직접 확정한다.
  관리자는 `/makeup` 월 달력에서 칩 클릭(⏱ = 근무는 끝났는데 미확정, 🗓 = 주말근무) →
  *확정 요청 보내기* 로 재촉하거나 실근무 시각을 직접 고친다(관리자가 최종 권한).
  결시보강은 *수당 반영* 을 직접 고른다(⚠ 표시가 판단 대기) — 정한 뒤 확정 요청을 보낸다.
  확정 후 급여를 다시 산정하면 명세서에 반영된다.
- **근무를 하지 않은 건 내리기** → 신청자가 슬랙 목록의 *미실시* 버튼(또는 확정 모달의
  `이 근무를 하셨나요? → 아니요`). 근무 전에도, 수당 대상이 아니어도 누를 수 있다.
  되돌리려면 관리자가 `/makeup` 에서 상태를 `신청됨` 으로 바꾼다.
- **오버타임 지급 조건·내신 기간 수정** → `/makeup` 의 *수당 지급 조건* (`OvertimePolicy` / `ExamPeriod`).
  법정 배수보다 낮추면 저장 시 경고가 뜬다.
- **공휴일 채우기·고치기** → 설정 → *공휴일*. `HOLIDAY_API_KEY` 가 있으면 *공휴일 받아오기*,
  없으면 *초기 표로 채우기* 또는 직접 입력(임시공휴일·학원 자체 휴무일). 명령줄은 `npm run db:holidays-sync`.
  **해가 바뀌면 반드시 확인한다** — 표가 얇으면 그해 공휴일 근무에 가산이 안 붙는다(화면이 경고한다).
- **퇴직급여 확인** → `/severance` 에서 연·월을 골라 본다. 행을 누르면 산입·제외 항목이 금액과
  사유까지 펼쳐진다. **산입 범위 변경** → 같은 화면의 *⚙ 산정 조건* (`SeverancePolicy`).
  법정 하한(§20①, 연간 임금총액의 1/12)에 못 미칠 설정이면 저장 시 경고가 뜬다(막지는 않는다).
- **월별 기준급여 정정** → `/severance` 에서 행 클릭 → *기준급여 수정*(사유와 함께 저장).
  그 달 급여보다 우선하므로 필요가 없어지면 *지정 해제* 로 되돌린다.
- **도입 이전 퇴직급여 채우기** → `npm run db:severance-migrate` (미리보기) →
  `APPLY=1 npm run db:severance-migrate`. 계약 이력에서 월별 기준급여를 추산해 넣는다.
  다시 추산하려면 `REDO=1`(관리자 지정분은 지켜진다).
- **계약 종료 예고·생일 알림 조정** → 설정 → *경영지원 알림*. 받을 부서·채널, 며칠 전에,
  보낼 시각을 고친다. *지금 보내기* 로 확인해 볼 수 있다(계약 예고는 그때도 한 번 쓴 것으로 친다).
- **슬랙 명령 추가** → `app/api/slack/command/route.ts`.
- **시간기록표 양식 변경** → `lib/timesheet.ts`(파서·주휴 산정, 테스트 있음) + `/api/payroll/timesheet`.
  휴게 30분은 Employee.breakPaid 로 유급/무급.
- **인센티브 입력은 세 갈래**다 — `인센티브 = 인원 기준 + 매출 기준 + 직접 입력분`.
  ① **인원 기준 자동산정**: `(학생수 − 기준인원) × 기준금액`. 기준값(`incThreshold`/`incPerStudent`)은
  계약에 있고, 학생수는 급여 화면의 `학생/매출` 칸이나 **학생 명단 엑셀**(회차 비례 가중)에서 온다.
  ② **매출 기준 자동산정**(`incRevenuePercent`): `담당 학생 수강료 매출 × 배분율`. 매출은 명단 엑셀에서
  온다(`classRevenue`). 완전비율제의 사업소득과 **같은 값을 쓰되 계약의 율만 다르다**.
  ③ **금액 직접 입력**(`incentiveManual`): 급여 화면의 `인센티브` 칸. 자동산정분 **위에 더해진다**.
  기준값을 계약에 안 넣었거나 그 달만 조정할 때 쓴다. 예전엔 엔진에만 있고 화면에 연결이 안 돼
  항상 0 이었고, 그래서 인센티브를 **상여 칸에 넣어 쓰던 흔적**이 있다(명세서에 '상여금' 으로 찍힌다).
  학생수·매출·직접 입력분 모두 배치 재실행 때 보존된다(입력 없이 API 를 부르면 지워지던 것을 고쳤다).
- **직원 계좌 채우기** → 직원 관리 → *명단 일괄 등록* → **계좌 등록** 탭에 은행 대량이체 파일을 올린다.
  한 명씩은 직원 상세 → *수정* 의 **급여 이체 계좌**(은행·계좌번호·예금주·퇴직유보금 통장).
- **퇴직자를 급여 시트에서 빼기** → 직원 정보에 **퇴사일만 넣고** *급여 일괄 산정* 을 누른다.
  그 달 재직 기간이 없으면 시트에서 내려가고, 누가 왜 빠졌는지 창으로 알려 준다.
  이미 명세서를 보낸 달이면 내리지 않고 알리기만 한다(잠금 해제를 거쳐야 한다).
- **퇴직자에게 남은 지급분 정산** → 급여 화면의 *＋ 직원 추가* 로 그 달 시트에 올린다.
  기본급은 0 원이므로 `상여`·`미사용` 칸이나 인센티브 칸에 정산액을 직접 넣는다.
  **그 달에도 재직한 것이라면 여기가 아니라 퇴사일을 고친다** — 그래야 기본급이 제대로 잡힌다.
- **인센티브 산정 변경** → `lib/incentive.ts`(재원계수·엑셀 명단 파서, 테스트 있음) + `/api/payroll/incentive`.
  월중 입학·전출·퇴원 학생은 정수 1이 아니라 **회차 비례**(재원계수 = 회차÷8, 상한 1)로 계산.
  인센티브 = (Σ재원계수 − 기준인원) × 기준금액. 1회당 단가 = 기준금액÷8(만근 8회).
  명세서에 「인센티브 산정 내역서」가 첨부문서로 자동 첨부(`incentiveDetailHtml`).

- **학생 명단은 두 갈래이고 산정식이 아예 다르다**(`lib/incentive.ts`, 테스트 있음).
  어느 쪽인지는 **매출 열의 유무**로 갈린다(`isRevenueRoster()`) — 명단에 열이 있는지가 진실이고
  급여형태로 넘겨짚지 않는다(같은 월급+인센티브라도 학원마다 다르게 쓴다).
  - **인원 기준**(`summarizeIncentive`) — 「26년 7월」 양식. `(Σ재원계수 − 기준인원) × 기준금액`.
    같은 표를 좌·우 두 벌 늘어놓고, **왼쪽 벌이 기준 인원**, 오른쪽 벌이 초과분이다.
    **관리시트와 답이 갈릴 수 있다 — 기준 인원을 무엇으로 채우느냐가 다르다.**
    시트는 왼쪽 벌에 **이름이 기준 인원수만큼** 있으면 채운 것으로 보고 오른쪽 벌만 지급한다.
    시스템은 계약서 제3조 문언대로 **명단 전체의 재원계수 합**에서 기준 인원을 뺀다.
    왼쪽 벌에 중도 입학·전입 학생(계수 <1)이 섞이면 계수 합이 기준에 못 미쳐 그만큼 적게 나온다
    (최은희 26년7월: 왼쪽 40명이 계수로는 38.125 → 시트 600,000원 / 시스템 412,500원).
    **시스템 방식을 유지하기로 정했고**(2026-08 결정), 대신 업로드할 때 시트 금액과 다르면
    차액과 사유를 경고로 띄운다 — 조용히 지나가면 시트보다 적게 지급된다.
    시트 금액에 맞추려면 급여 화면의 `인센티브` 칸에 차액을 직접 넣는다.
    시트가 낸 금액은 `sheetTotalAmount` 로 읽는다 — 매출 기준은 TOTAL 행에서,
    인원 기준은 TOTAL 행이 없으므로 **학생별 인센티브 칸의 합**에서.
  - **매출 기준**(`summarizeRevenueShare`) — 「사업소득/인센티브 상세 내역 - 26년7월」 양식.
    `Σ 수강료 매출 × 배분율`. **월중 입·퇴원 비례가 매출 금액에 이미 반영**돼 있으므로
    (신규 6회 → 380,000 이 아니라 266,000) 재원계수를 다시 곱하지 않는다 — 곱하면 두 번 깎인다.
    회차는 근거로만 함께 적는다.
  **배분율은 명단이 아니라 계약에서 온다** — 완전비율제는 `ratioPercent`, 월급+인센티브는
  `incRevenuePercent`. 명단에 적힌 율(`sharePercent`)은 **대조용으로만 읽어** 다르면 경고를 남긴다
  (보수조건은 계약만 고친다는 규칙. 시트의 율이 바뀌었다고 조용히 따라가면 계약과 어긋난 채 지급된다).
  총액은 **Σ매출에 율을 곱해 한 번 반올림**하고 학생별 잔차는 마지막 학생이 흡수한다 —
  급여 엔진의 비율제 산식과 같은 자리에서 반올림해야 명세서와 첨부 내역서가 원 단위까지 맞는다.

- **명단 엑셀은 탭 하나가 강사 한 명이고, 그 안에서 달이 오른쪽으로 이어진다**
  (`parseRosterWorkbook()` → `findMonthBlocks()`). 3월 A:P, 4월 R:AG … 식으로 **가로로** 붙는다.
  - **대상 월은 화면에서 고른 연·월로 정한다** — 파일에 늘 여러 달(많게는 28개월)이 들어 있어
    '파일에서 자동 감지' 로는 어느 달인지 정할 수 없다. 시간기록표와 다른 점이다.
  - 제목 줄의 위치가 시트마다 다르므로(1행 또는 2행) **제목이 가장 많이 잡힌 줄**을 제목 줄로 본다.
  - 강사명은 블록 안의 「○○○ 선생님」, 없으면 **탭 이름**을 쓴다.
  - **좌·우 두 벌의 경계는 '이름' 열 사이의 중간이 아니라 열 제목이 되풀이되는 자리**다.
    중간으로 끊으면 앞 벌의 퇴원일·인센티브 열이 뒤 벌 범위에 들어가 **뒤 벌이 앞 벌의 퇴원일을
    자기 것으로 집어 들어** 뒤 벌 학생의 퇴원 회차가 통째로 사라진다.
  - **입학·퇴원 칸에 회차가 둘 다 있으면 큰 쪽**이 그 달 실제 회차다(「입학 7/16(0회) · 퇴원 7/21(1회)」 → 1회).
    작은 쪽을 쓰면 관리시트가 인정한 회차가 0 으로 깎인다.
  - **퇴원·휴원인데 회차가 없으면 만근이 아니라 0회**다. 재원 학생의 빈 칸은 '만근이라 적을 게 없다',
    퇴원 학생의 빈 칸은 '그 달에 나온 수업이 없다' 로 뜻이 정반대다.

- **명세서 뒤에 붙는 산정 내역서**(`rosterDetailHtml`, `lib/documents-pay.ts`, 테스트 있음).
  대상은 **완전비율제와 월급+인센티브 둘뿐**이고, 명단이 저장돼 있는 달에만 붙는다.
  - **자동산정을 쓰는 달은 저절로 따라붙는다** — 명단이 이미 DB(`IncentiveStudent`)에 있기 때문.
  - **금액을 직접 넣는 달은 관리시트를 올리면 붙는다** — 올리면 명단이 저장되고 그때부터 붙는다.
  - **명단이 없으면 첨부 없이 명세서(또는 사업소득 지급명세서)만 나간다.** 붙일 근거가 없는데
    빈 내역서를 붙이면 '없는 별첨' 을 가리키게 된다. 계약에 배분율이 없을 때도 붙이지 않는다.
  - 매출 기준과 인원 기준이 **같은 골격·같은 2단 배치**를 쓴다(7·8열만 회차·인센티브 대신
    매출·배분액). 자동으로 붙는 문서와 업로드로 붙는 문서가 다르게 생기면 같은 회사 문서로 안 읽힌다.
